# CLAUDE.md

このリポジトリで作業するときに、まず読むファイル。
**合意済みの決定事項を集約し、同じ議論を繰り返さないためにある。**

決定を変える場合は、このファイルを更新し、理由を ADR に残すこと。

---

## 1. プロジェクトの概要

架空企業「株式会社サンプル」（従業員 100 名・単一事業所）の勤怠管理システム。
個人開発であり、**ポートフォリオとしての提示** と **設計手法の習得** を目的とする。

主題は機能の多さではなく、次の 2 点である。

1. 業務ルールを型で表現し、ありえない状態をコンパイラに検出させる
2. ありえない状態をデータベース制約で物理的に作れなくする

---

## 2. 確定事項

以下は合意済み。**独断で変更しないこと。**

### 2.1 業務要件

| 項目 | 決定 | 出典 |
| --- | --- | --- |
| 利用形態 | 架空の 1 社向け社内システム。マルチテナントは扱わない | 合意 |
| 労働時間制度 | 固定時間制 + フレックスタイム制（清算期間 1 か月、コアタイム 11:00–15:00） | 要件定義書 3.1 |
| 労働時間の集計単位 | **1 分単位。丸めを行わない** | 要件定義書 BR-01 |
| 休憩 | 打刻で記録する。法定休憩に不足する場合は警告のみ | 要件定義書 BR-08 |
| 組織 | 部署ツリー（本部 → 部 → 課）。所属は有効期間を持つ | 要件定義書 BR-11 |
| 承認 | BR-11 が定める承認者による 1 段階承認。自己承認は禁止し、遡っても得られない場合は人事が承認する | 要件定義書 BR-11 |
| 兼務 | **扱わない。** 1 社員の所属は同時に 1 部署。ただし部署長の兼任は認める | 要件定義書 BR-11 補足 |
| 雇用形態 | **持たない。** 全社員の所定労働時間は 1 日 8 時間。短時間勤務は対象外 | 要件定義書 3.1 |
| 認証 ID | **社員番号**。メールは退職者の再割り当てと衝突するため使わない | 要件定義書 7 |
| 締め | 月末締め。人事担当が実行。締め後は変更不可 | 要件定義書 BR-10 |
| 年次有給休暇 | **付与基準日は入社日**（法定どおり）。出勤率 8 割で付与、時効 2 年、消化は先入先出、1 日単位のみ | 要件定義書 BR-14〜BR-17 |
| 比例付与 | **対象としない。** 全社員 1 日 8 時間・短時間勤務は対象外なので**該当者が存在しない**。扱うなら BR-04・BR-05 と併せて要件から改訂する | 要件定義書 BR-14 |
| 年 5 日の取得義務 | **可視化までを担う。使用者による時季指定は実装しない。** 未達でも取得・提出・承認・締めを止めない | 要件定義書 BR-17 |
| 認証・認可 | Spring Security（セッション） + ロール認可。**ログインは `POST /api/sessions`（既定のフォームログインを使わない）。`APPROVER` は認証時に部署長の事実から導出する** | 要件定義書 4 / API 設計書 3.9 |

業務ルールの詳細は [要件定義書](doc/01_要件定義/要件定義書.md) の 6 章（BR-01〜BR-12）が正。
**実装とテストは必ずこの ID を参照すること。**

### 2.2 技術スタック

| 領域 | 採用 | 備考 |
| --- | --- | --- |
| 言語 | Java 21 | `record` / `sealed interface` / パターンマッチングを積極的に使う |
| フレームワーク | Spring Boot 4.1.1 | 3.5 系は OSS サポート終了済み |
| ビルド | Gradle 9.7 (Kotlin DSL) | |
| DB | **PostgreSQL 16** | `EXCLUDE` 制約・範囲型・パーティション・配列型・生成列を使う。設計書の DDL はすべて 16 で検証しているので、採用版を 16 にそろえる |
| マイグレーション | Flyway | |
| O/R マッパー | Spring Data JPA | `infrastructure` 層の内側でのみ使う。**追記専用・まるごと入れ替えの表（打刻・日次勤怠の内訳）は `JdbcTemplate`**（同一性の管理も変更の追跡も要らないため） |
| テスト | JUnit 5 / AssertJ / Testcontainers / ArchUnit | 統合テストで H2 は使わない |
| フロントエンド | React 19 / TypeScript / Vite | |
| パッケージ | `jp.co.sample.kintai` | Group `jp.co.sample` / Artifact `kintai` |

**Lombok は使わない。** `record` があれば getter・equals・hashCode は不要であり、
`@Data` はドメインオブジェクトを可変にする方向に働くため。

### 2.3 アーキテクチャ

| 項目 | 決定 | 根拠 |
| --- | --- | --- |
| パッケージ構成 | **業務コンテキスト優先**。`attendance` / `employee` / `workrule` / `approval` / `leave` / `shared` を最上位に置き、その中を 4 層に分ける | [ADR 0001](doc/05_ADR/0001_パッケージを業務コンテキスト優先で分割する.md) |
| ドメインと永続化 | **分離する**。ドメインは `record`、永続化は `@Entity`、両者を変換する | [ADR 0002](doc/05_ADR/0002_ドメインモデルとJPAエンティティを分離する.md) |
| 層の依存 | `presentation → application → domain`、`infrastructure ⇢ domain`（ポートを実装） | アーキテクチャ設計書 3.3 |
| トランザクション境界 | `application` 層の公開メソッド。`open-in-view` は無効 | アーキテクチャ設計書 6.1 |
| 例外 | ドメイン例外 → `@RestControllerAdvice` → RFC 9457 (Problem Details) | アーキテクチャ設計書 6.2 |
| 時刻 | ドメインは `LocalDateTime`（壁掛け時計）、DB は `timestamptz`。変換は `BusinessZone` に集約 | アーキテクチャ設計書 6.3 |
| 構造の強制 | ArchUnit（AR-01〜AR-09）。違反したらビルドを落とす。**ルール自体を `ArchRuleSelfTest` が検査する**（わざと違反したクラスに当てて落ちることを確かめる） | アーキテクチャ設計書 8 |
| 業務エラーの表現 | `shared.domain.DomainException` を継承し、`errorCode()` で Problem Details の `type` を返す。実装の不備（null・桁あふれ）は継承させない | アーキテクチャ設計書 6.2 |
| 取りうる値が法で固定された概念 | `enum` にする。`record` + 実行時検証だと、型の上では不正な値を渡せる（`NightWindow` がこの形） | CLAUDE.md 4.3 |
| BR-11 の責務分割 | `employee.OrganizationChart` が**事実の導出**、`approval.ApproverPolicy` が**承認の業務ルール** | アーキテクチャ設計書 6.4 |
| 期間の表現 | **すべて半開区間 `[from, toExclusive)`**。退職日は最終在籍日なので `plusDays(1)` で変換する | 社員・組織 ドメインモデル設計書 2.2 |
| 時刻の生成元 | `created_at` / `updated_at` は **DB の `now()`**（監査のため偽装できない）、業務上の日時は **`Clock`**（テストで固定するため） | 社員・組織 DB設計書 2.2 |
| コンテキスト間の逆向き問い合わせ | **ポートを `shared.domain` に置き、実装を提供側に置く。** 新しい辺を増やさない。`MonthClosureQuery`（締め判定）がこの形 | アーキテクチャ設計書 4 |
| 共有語彙の置き場所 | 2 つ以上のコンテキストが使う概念（`PremiumType`・`EmployeeId`・`DateRange`）は `shared.domain` へ。片側に置くと依存が循環する | アーキテクチャ設計書 4 |
| 履歴を持つマスタ | **系列（不変の識別子）と版（有効期間つき）に分ける。** 参照する側は系列を指す。就業規則がこの形 | 就業規則 DB設計書 2.1 |
| 適用開始日 | 月初日、**または当該社員の入社日**。月初日だけに限ると月中入社の初月が締められない | 就業規則 API設計書 2.3 |
| 法定値の制約 | 下限だけでなく **上限**も置く。法定労働時間・深夜帯を自由に動かせると割増の対象そのものが消える | 就業規則 DB設計書 2.2 |
| 設計書を書き終えたら | [設計規約チェックリスト](doc/02_詳細設計/00_共通/設計規約チェックリスト.md) を上から確認してからコミットする | レビュー第 2 回 |
| **実装を変えたら** | **その根拠になった設計書の節を同じコミットで直す。** 別コミットに回すと必ず片方だけが古くなる | レビュー第 3 回 |
| 永続化アダプタ | ドメインの record と JPA エンティティを変換する。エンティティは**パッケージプライベート**で層の外に出さない | ADR 0002 |
| 番兵と NULL の写像 | `infrastructure` の責務。`DateRange.UNBOUNDED_END` は PostgreSQL の `date` に入らないので、1 か所（`Periods`）に閉じる | CLAUDE.md 落とし穴 35 |
| 時点解決 | **SQL で書き直さない。** 行を読んでドメイン（`EffectiveWorkRule`）へ渡す。2 か所に実装があると、どちらが正しいかを 2 か所で保つことになる | レビュー第 3 回 |
| 判別値と分岐 | 分岐は `sealed interface` に対する網羅性検査つき `switch`。enum（`WorkingTimeSystemType` など）は**永続化と表示のためだけ** | 就業規則 ドメインモデル設計書 2.1 |
| API の応答 | **他コンテキストが所有する概念を返さない。** 社員番号・氏名・部署は `employee` から引く | 設計規約チェックリスト 3 |
| 依頼者の受け渡し | **`shared.domain.Requester` を引数で渡す。** `SecurityContextHolder` を `application` 層から読まない。読むとユースケースが「誰の依頼か」を差し替えられなくなり、バッチからも呼べなくなる（落とし穴 42） | M1-c/A |
| 閲覧範囲の判定 | **`shared.domain.EmployeeVisibility`（ポート）。実装は組織を持つ `employee` 側。** 「配下部署か」は組織と基準日に依存するので Spring Security の認可設定には置けない | 要件定義書 4.1 |
| 業務エラーの HTTP 対応 | ドメインは `DomainErrorKind`（業務上の分類）だけを持つ。HTTP への対応づけは `presentation` の網羅性検査つき `switch`。`RULE_VIOLATION`=422 / `CONFLICT`=409 / `NOT_FOUND`=404 / `FORBIDDEN`=403 | アーキテクチャ設計書 6.2 |
| 実装の不備の応答 | `IllegalArgumentException` などは**メッセージを応答に載せない。** ログへ出して 500。制約名やテーブル名は内部の構造であり外に出さない | アーキテクチャ設計書 6.2 |
| 締め状態の問い合わせ | **`MonthClosureQuery` は「打刻を受け付けるか」と「訂正申請を受け付けるか」を分ける。** 1 つにまとめると提出済みの月で真になり、**本人が提出後に直接打刻できてしまう**（承認者が見た内容と確定する内容が食い違う） | 05 ドメインモデル設計書 2.1 |
| 月次清算の再計算 | **人事の指示のときだけ版を突き合わせる。** システムが契機で行う再計算（提出・訂正の承認）は誰かが見ている値ではないので、突き合わせる相手がいない | 04 API 設計書 3 |
| 打刻の成否と計算の成否 | **分けて返す**（`calculationStatus`）。就業規則の未設定を理由に打刻を拒否すると、働いた事実が残らない | CLAUDE.md 落とし穴 19 |
| 勤務日と暦日 | 労働時間の帰属は**勤務日**（BR-03）、法定休日労働の判定は**暦日** | 日次集計 ドメインモデル設計書 2.4 |
| 要件に無い概念 | 設計で必要になったら、**要件を先に改訂する**。要件定義書は版 1.3 | レビュー第 2 回 |
| 楽観ロックの版 | **行の作成時に 1 を入れる。** 既定値の 0 に任せると「行が無い」と「作られたばかり」が同じ値になり、古い版がそのまま一致する | CLAUDE.md 落とし穴 57 |
| 一括操作の失敗 | **社員ごとの事情は結果へ、依頼そのものの不備は例外へ。** 前者を例外にすると 1 件で全件が巻き戻り、後者を結果にすると権限不足に気づけない | 申請承認 API設計書 2.7 |
| 一括操作の実装場所 | **別のクラスに置く。** 同じクラスから `@Transactional` を呼ぶとプロキシを通らず、1 トランザクションになる | CLAUDE.md 落とし穴 59 |
| 訂正の代理申請 | **認めない。** 訂正は本人の意思表示であり、人事でも代わりには出せない。認めると「本人が申請していない訂正」が生まれる | 申請承認 API設計書 3.1 |
| シナリオテストの経路 | **HTTP を通す。** アプリケーションサービスを直接呼ぶと 1 シナリオが 1 トランザクションになり、締めの排他と楽観ロックが検証できない | 結合テスト仕様書 7 の #2 |
| 全社共有の表の締め判定 | **`isClosedForAnyone` を分けて置く。** カレンダーは 1 つしか無いので、社員単位の判定では他人が締めた月を守れない | CLAUDE.md 落とし穴 72 |
| 他コンテキストの `application` | **図にある辺に沿うかぎり呼んでよい**（AR-06 は `infrastructure` / `presentation` だけを禁じる）。複数のリポジトリを束ねる手順は `domain` に置けず、写すと片方が古くなる（落とし穴 67）。逆向きはポートを `shared.domain` に置く | アーキテクチャ設計書 4 |
| 一覧に版を載せるか | **詳細だけに載せる。** 一覧で行ごとに引くと、社員数ぶんの問い合わせと認可判定が重複する。版が要るのは更新する 1 人だけ | 社員・組織 API設計書 3.12 |
| 訂正の承認者 | **`workDate` が属する月の承認者。** 月次と食い違うと「月次は承認できるが訂正は承認できない」上長が生まれる | 申請承認 ドメインモデル設計書 4.1.2 |
| 締め済みと承認済み | **別のエラーにする**（`month-already-closed` / `month-not-editable`）。承認済みは承認を取り消せば直せるので、利用者への案内がまったく違う | 申請承認 API設計書 3.1 |
| 提出の事前条件 | **「打刻があるのに日次勤怠が無い勤務日」が無いこと。** 「所定労働日すべてに行があること」ではない（落とし穴 90）。判定は `attendance` が持ち（`MonthlySettlementService.requireCalculable`）、提出はそれを呼ぶ。写すと片方が古くなる（落とし穴 67） | 05 ドメインモデル設計書 2.3 |
| 年次有給休暇の付与 | **行として実体化する。参照時に導出しない。** 実績が訂正されると過去の付与日数が黙って変わる。契機は `grantAsOf(asOf)` の 1 本だけで、到来済みの未処理分をすべて作る（冪等） | 06 ドメインモデル設計書 2.5 |
| 年休の残日数 | **列を持たない。付与と配分から導く。** 失効も「付与日 + 2 年」だけで決まるので行にしない。逆に**配分（どの付与から消化したか）は行で残す**（再判定で配分先が入れ替わらないため） | [ADR 0006](doc/05_ADR/0006_残数は導出し配分だけを行として残す.md) |
| 年休の日と日次勤怠 | **`daily_attendances` に行を作らない。** 同じ事実が 2 か所に載る。BR-05 の不足時間は**所定労働日数から年休の日を引く**ことで、固定時間制とフレックスの両方が 1 か所で正しくなる | 06 ドメインモデル設計書 6 |
| 年休の承認と月次勤怠 | **提出済みの月は下書きへ戻す**（`REVERT_BY_LEAVE`）。訂正の承認とまったく同じ形。`REVERT_BY_CORRECTION` を流用すると、何が原因で戻ったのかが証跡から読めない | 06 API設計書 3.2 |
| 年休の代理申請 | **認めない。** 時季指定は本人の意思表示であり、人事でも代わりには出せない（訂正申請と同じ判断） | 06 ドメインモデル設計書 4.2 |
| 出勤率の判定 | **整数の掛け算で比べる**（`attended * 10 >= total * 8`）。Java の `/` は整数除算なので 120/150 が 0 になり、`double` へ寄せると `0.8` という閾値がコードに現れる。法が定めているのは「5 分の 4」である。全労働日 0 の期間は満たしたものとして扱うが、**これは付与日に在籍している社員にしか当てない**（退職者は必ず全労働日 0 になる） | 06 ドメインモデル設計書 2.4 |
| 年休の付与対象 | **付与日に在籍している社員だけ。** 除かないと退職者に毎年 20 日が積み上がる。しかも退職者は全労働日 0 なので**出勤率の判定を必ず通る** | 06 ドメインモデル設計書 2.5 |
| 年休の取消 | **申請中はいつでも本人が／承認済みは前日まで本人が／当日以降は人事が理由を付けて。** 申請中に期限を設けると、決裁されないまま取得日と月末が過ぎた申請がどの状態にも遷移できなくなる。当日以降を訂正申請（BR-09）に委ねるのは誤りで、訂正申請が動かせるのは打刻だけ | 要件 BR-16 / 06 API設計書 3.4・3.5 |
| 年休の取下げと月の状態 | **申請中の取下げだけは対象月の状態を問わない**（締め済み・承認済みでも通す）。残日数も月次清算も動かさないので拒否する理由が無く、拒否すると遷移先の無い申請が残る。**副作用（配分・月次清算・下書きへの差戻し）を伴うのは承認済みからの取消だけ** | 要件 BR-16 / 06 ドメインモデル設計書 4.2・4.3 |
| すでに働いた日の年休 | **承認しない**（`leave-date-already-worked`）。**年休の日に出勤していたことは提出時の警告として示す。** 締め後は人事でも取り消せないので、締め前に気づく経路が要る | 要件 BR-16 / 06 API設計書 3.2・3.5 |
| 休業と出勤率 | **人事が「出勤扱いの日数」を理由つきで申告する。** BR-14 は労災・産育休を出勤扱いと定めるが休業を記録する機能が無く、そのままでは産休・育休の社員の付与が必ず 0 になる。**打刻を足して実績を整えさせない**（働いていない日の労働時間が一次証拠として残る） | 要件 BR-14 / 06 ドメインモデル設計書 2.4 |
| 適用済みマイグレーションの変更 | **書き換えない。** 新しい版で `ALTER TABLE ... DROP/ADD CONSTRAINT` する。書き換えると Flyway のチェックサムが壊れる | 06 DB設計書 3.4 |
| テストの置き場所と ID | **観点の ID は置き場所で振り直さない。** 複数のリポジトリを束ねないと成立しない規則（残日数・付与の実体化・対象月の状態・実労働の有無・在籍期間との交わり）は、設計書が `UT-` と呼んでいてもアプリケーション層 + 実物の PostgreSQL でしか確かめられない。ID を振り直すと同じ観点を 2 つの ID で追うことになる | 06 ドメインモデル設計書 7 |
| 保有日数と申請可能日数の絞り方 | **`remainingDays` は基準日で、`availableDays` は申請できる期間（当日 + 1 年）との重なりで絞る。** 受理判定は取得日で有効な付与を探すので、表示だけ基準日で絞ると**付与日が未到来の付与が落ちて「0 日と表示されるのに申請は通る」**。組み入れる期間は 1 か所で決める | 06 ドメインモデル設計書 3.4 |
| 依頼者の検査の順序 | **「誰の依頼か」を最初に見る。** 集約の検査（落とし穴 58 のために残す）に任せると、その前の検査が対象社員に対して実行され、締め状態・在籍・残日数がエラーの型から読み取れる | 06 API設計書 3.1 |
| 承認者が導出できない申請 | **人事が却下できる。承認は開けない。** 未来日の年休のあとに退職が登録されると承認者が決まらず、本人はログインできないので取り下げもできない。`ApproverPolicy` 自体は変えない（月次勤怠では起きない） | 要件 BR-16 / 06 API設計書 3.3 |
| 一括処理の 1 件の失敗 | **社員ごとに捕まえて結果へ入れる**（`failed`）。例外でループを抜けると以降の社員が一件も処理されず、結果も返らないので誰も気づけない | 06 API設計書 4.1 |
| 利用者が送る値の検証 | **`IllegalArgumentException` のまま素通ししない。** ドメイン例外に写して 422 で返す。素通しすると理由の載らない 500 になり、何を直せばよいか伝わらない | 06 API設計書 4.2 |
| 手続きを止めない知らせ | **応答の `warnings` に載せる。** 空なら項目ごと省く（落とし穴 76）。判定は事実を持つコンテキストに置く | 05 API設計書 2.2 |
| 承認者でないことのエラー | **`not-approver`（403）を一般の権限不足（`forbidden`）と分ける。** 承認者は組織と基準日から導かれるので、「ロールが足りない」のではなく「その月のその社員の承認者ではない」。05 と 06 で同じ型を使う | 05 API設計書 1.2 |
| 決裁の応答の版 | **読み直して返す。** 更新は SQL 側で版を 1 つ進めるので、遷移させた集約は古い版を持ったままである。返さないと画面は必ず 1 回 409 を踏む | 06 API設計書 3.2 |
| 決裁の応答に他コンテキストの集計を埋めない | **月次清算は含めない。** 清算の項目が増えるたびに年休の応答まで変わる。必要なら `GET .../monthly-settlements/{month}` を引く | 設計規約チェックリスト 3 |
| 自己決裁の検査の位置 | **認可の検査を「自己 → 承認者 → 業務」の順に並べる。** 集約の検査に任せると `ApproverPolicy` が本人を承認者から外すので `not-approver` が返り、自己決裁という事実が伝わらない。集約の検査は経路外のために残す（落とし穴 58） | 05 API設計書 IT-APV-36 |
| 一覧を返す API のロール | **ロールで一律に拒まない。** 閲覧範囲で絞れば、一般社員が呼んでも自分のぶんだけが返る。拒むと、自分があと何日年休を取る必要があるかを本人が確かめられなくなる | 06 API設計書 1 |
| 期限までの残り日数 | **`deadline`（閉区間の最終日）まで数える。** 半開区間の上限まで数えると 1 日多くなる。`expiresOn` と `deadline` で区間の扱いが違うので必ず取り違える | 06 API設計書 2.1 |
| 給与連携の範囲 | **時間（分）と日数だけを渡す。金額も割増率も出さない。** 率は法で決まっており給与側も持っている。本システムが書くと法改正のたびに 2 か所を直すことになる | 要件 BR-18 |
| 深夜の渡し方 | **合計のまま渡す。区分ごとのクロス集計を持たない。** 深夜割増は他の割増への**上乗せ**（+25%）なので、排他区分の時間と深夜の合計があれば受け取った側が計算できる。75% を単一の率にしようとすると、60 時間に到達した時点の特定が要り、週 40 時間超とフレックスの時間外が紐づかないので解けなくなる | 要件 BR-18 / 04 ドメインモデル設計書 3.4 |
| 時間外と不足の相殺 | **しない。両方をそのまま渡す。** 相殺すると同じ時間が「割増の対象」と「欠勤の穴埋め」に二重に使われる。相殺するかどうかは賃金規程の判断である | 要件 BR-18 |
| 基礎賃金と割増の切り方 | **別の切り方であり、入れ子になっていない。** 基礎賃金は所定との比較で決まり（所定内＝月給に含む／所定超＝1.0 倍の追加支払）、割増は 37 条の区分で決まる。**所定に届いていないのに時間外労働が生じる月がある**（所定総 > 法定総枠）ので、「割増の付かない時間」を所定内と法定内残業へ割ると基礎賃金を二重に払う | 要件 BR-18 / 07 ドメインモデル設計書 2.1 |
| 所定内労働時間の求め方 | **`所定総 − 不足時間`。** 不足は制度ごとに数え方が違うが、どちらも「所定総 − 実績（所定で頭打ち）」の形なので、引くと所定の範囲内で働いた時間になる。**式は `MonthlySettlement` に置く**（不足の定義を持つのは `attendance`）| 07 ドメインモデル設計書 2.2 |
| 割増賃金の基礎額 | **月次の CSV では渡せない。年ごとの別表にする。** 労基則 19 条 1 項 4 号の「1 年間における 1 か月平均所定労働時間数」は会社カレンダーからしか出せず、1 か月ぶんの行には入らない | 要件 BR-18 |
| 出力の記録 | **「出力済み」を状態として持たないことと、実行の記録を残さないことは別。** 何度でも出せる（状態を持たない）が、誰がいつどの月を出したかは残す。全社員の賃金データを外部へ持ち出す操作である | 要件 BR-18 |
| 一括出力から除いた社員 | **理由を分けて返す**（未提出 / 未承認 / 未締め / **提出できない** / **打刻が 1 件も無い** / **社員番号の重複**）。除外はその月の給与が出ないことを意味し、一括締めと違って支払期日を過ぎたら取り返せない。**「打刻が無い」を月次勤怠の行の有無で判定しない**（行が無いことは「下書き」を意味する）| 要件 BR-18 / 07 ドメインモデル設計書 3.2 |
| 給与へ渡す日数 | **所定労働日数は年休を引く前。欠勤日数は別に渡す。** 控除後を日額の分母にすると年休を取った月ほど 1 日あたりの控除が大きくなる。出勤日数は法定休日の出勤を含むので `所定労働日数 − 出勤日数` では欠勤日数にならない | 要件 BR-18 |
| CSV の名寄せの鍵 | **社員番号は在籍中しか一意でない。** 部分一意インデックス（`WHERE retired_on IS NULL`）なので、月中退職者の番号を同月中に再割り当てすると**同じ CSV に同じ番号の行が 2 つ**できる。その 2 人を除外し、人事に割り当て直させる | 07 ドメインモデル設計書 3.2 |
| 出力の記録 | **対象社員を行として残す。** 値が動かないことと**行の集合が動かないこと**は別で、記録のあとに残りの社員を締めると同じ記録から取れる CSV の行数が増える。件数は行から数え、列として持たない | 07 DB設計書 1 |
| 割増賃金の基礎額の分母 | **カレンダー未登録の年度は拒否する。** 未登録の日は所定労働日として扱うので年間 365 日になり、月平均が 1.5 倍・**単価が法定を約 33% 下回る**。出力に使った月平均は記録に残す（年度の値は後から動く）| 07 ドメインモデル設計書 4 |
| 時間外と対象労働時間 | **時間外 ≤ 対象労働時間を集約の不変条件にする。** 割増の区分は実労働を分割するもので、外から生えない。破り方は「月へ振り分ける基準が項目ごとに違う」形で現れる | 04 ドメインモデル設計書 3.5 |
| 給与連携の置き場所 | **新しいコンテキスト `payroll`。`attendance` に置かない。** 出力の 1 行は社員番号で始まるので、置いた瞬間に 04 の「他コンテキストが所有する概念を返さない」を破る。同じパッケージに守る API と破る API が並ぶと、規約は「どちらでもよい」に退化する | [ADR 0007](doc/05_ADR/0007_給与連携を新しい業務コンテキストにする.md) |
| 日額の分母と所定総の根拠 | **所定労働日数を 2 つ渡す。** 日額（月給 ÷ これ）の分母は**暦月**、所定総の根拠は**清算期間**（暦月 ∩ 在籍期間）。月中入社・退職の月では食い違い、清算期間のほうを分母にすると控除が約 1.9 倍になる（労基法 24 条）。しかも暦月の所定労働日数は他のどの列からも復元できない | 要件 BR-18 / 07 ドメインモデル設計書 2 |
| 清算期間と賃金計算期間 | **別のものである。** 賃金計算期間（労基則 54 条 1 項 3 号）は賃金規程が定めるもので、月末締めでは**暦月**。CSV の清算期間は在籍期間で切ってあるので、そのまま賃金台帳へ写せない | 要件 BR-18 |
| 年間の所定の数え方 | **日ごとに「その日に適用されている系列」を見る。系列ごとに 1 年ぶんを足してから比べない。** 年度の途中で新設した系列は、それ以前の日に版を持たないのが正常である。系列単位で年度の全所定労働日ぶんの版を要求すると、**その年度のどの月も永久に出力できない**（落とし穴 131）| 07 ドメインモデル設計書 4.2 |
| 出力に使った分母の保護 | **「出力したか」ではなく「分母が動くか」で判定する。** 書き込んだあとに数え直し、記録した値と突き合わせる。前者だと分母を動かさない訂正まで止まり、就業規則の適用（分母のもう一方の入力）は素通りする。**1 行も出なかった記録は凍結しない**（動作確認で 1 回叩いただけで年度が固まる）| 07 ドメインモデル設計書 4.5 |
| 欠勤日数の数え方 | **引き算で導かない。** 年休を承認した日に出勤した月ではその日が年休にも実労働にも数えられ、`Math.max(0, …)` は重なりを隠すだけである。定義（所定労働日のうち、年休でも実労働でもない日）をそのまま数える | 04 ドメインモデル設計書 |
| 一括操作の入力の検証 | **`DateRange` の compact constructor や `Collectors.toMap` に任せない。** 期間の逆転もキーの重複も理由の載らない 500 になる。**後勝ちで畳まない**（どちらを意図したのか決められない）。期間の外の個別指定も**黙って捨てない** | 02 API設計書 3.2 |
| ログの役割 | **監査証跡は DB、障害調査はログ。** 5 年保持（要件 7）の対象は `approval_events` などの表であって、アプリケーションログではない。ログは一時的なものとして扱い、本文・パスワード・クエリ文字列を書かない | アーキテクチャ設計書 6.5 |
| 認証の記録 | **成功も失敗も理由つきで記録する。** 応答で理由を区別しない（総当たり対策）ことと、記録しないことは別である。ロックアウトを設けていない以上、記録が総当たりに気づく唯一の手段になる。**ロックアウトは入れない**（社員番号を知っている者が任意の社員を締め出せる）| 要件 BR-13 |
| アクセスログの位置 | **Spring Security より外側に置く。** 内側だと 401 で弾かれた要求が一切残らない。ただし戻った時点では `SecurityContextHolder` が空なので、利用者は Security の内側から要求属性へ書き写す | 08 障害時の初動 |
| 死活監視の口 | **`/actuator/health` だけを未認証で開ける。** `anyRequest().denyAll()` は actuator も塞ぐので、開けないとコンテナの `HEALTHCHECK` が起動直後から永久に失敗する。`env` と `beans` は塞いだまま（設定値と依存の一覧は監視に要らず攻撃者には有用）| 08 デプロイ手順書 |
| 本番の DB 接続 | **既定値を持たない。未設定なら起動を落とす。** 開発用の既定値を引き継ぐと、環境変数を渡し忘れたコンテナが黙って localhost の開発 DB を探しに行く（落とし穴 123 と同型）| ADR 0009 |
| インスタンス数 | **1 つ。スケールアウトしない。** セッションはインメモリのまま。100 名・同時 50 で増やす理由が無く、増やすと運用対象が 1 つ増える。夜間に止めて入れ替える | [ADR 0009](doc/05_ADR/0009_1インスタンスで運用する.md) |
| 「次に何ができるか」 | **サーバが返す。** `availableActions`（BR-02）と `canSubmit` / `canApprove` / `acceptsTimeClock` / `acceptsCorrectionRequest`（BR-10・BR-11）。画面が状態から組み立て直すと、状態機械と承認者の規則が 2 か所に生まれる。判定は `submit` / `approve` が実際に課す条件をそのまま写す（押せるのに弾かれるボタンを出さない）| 90 画面設計書 1.1 |
| 給与へ渡す値の計算式 | **`payroll` に持たせない。** 所定内労働時間は不足時間の定義に依存し、その定義を持つのは `attendance` である。`MonthlySettlement` の導出メソッドにする（落とし穴 67）| 07 ドメインモデル設計書 2.2 |
| 画面が持つ判断 | **業務ルールを複製しない。** 「次に何ができるか」（`availableActions`・`canSubmit`・`canApprove`）はサーバが返す。画面に書くと 2 か所で保守することになる | 画面設計書 1.1 |
| 画面の API の型 | **手書き。** ただし**コンパイラに守られていない**（落とし穴 143）ので、実物のバックエンドへ当てる通しのシナリオを必ず置く | 画面設計書 7 |
| 業務に届く前の誤り | **`presentation` で Problem Details に写す。** 必須パラメータの欠落は 400、URL の綴りの誤りは 404、型の不一致は 400。既定のままだと**本文の無い 403** になる | アーキテクチャ設計書 6.2.1 |
| 期間を受け取る API | **上限を置き、`DomainException` で拒む。** `DateRange` の compact constructor に任せると理由の載らない 500 になる（落とし穴 105）| 03 API設計書 3.1 |
| 一覧の応答に合計を載せるか | **分の合計だけ。日数は載せない。** 合計の目的は「排他区分の合計＝実労働」を受け取った側が検算できることである。日数は年休と法定休日で数え方が変わるので、行を数えると**月次清算と違う定義の同名の数**が並ぶ | 03 API設計書 3.1 |
| 応答の日時の桁 | **秒まで。変換の側（`JsonConfig`）で 1 か所にそろえる。** 打刻は `Clock`（ナノ秒）、証跡は DB の `now()`（マイクロ秒）と出どころが 2 つあるので、生成元ごとに切り詰めると片方が古くなる | 03 API設計書 2.2 |
| 画面の操作対象 | **一覧から開いた詳細の操作に、一覧側の絞り込み条件を使わない。** 対象は開いている集約から取る（落とし穴 152）| 画面設計書 8 |
| 設計書と実装の突き合わせ | **`check-endpoints.py` が設計書の API 一覧と実装の経路を突き合わせ、食い違ったら落とす。** テストは存在する API しか検査しないので、欠落はテスト数では検出できない（落とし穴 155）| doc/_tools |
| 系列の版の始まり | **0 から。** 月次勤怠は 1 から始める（落とし穴 57）が、あちらの危険は「行が無い月」を画面が版 0 として握れることにある。**系列は取得しないと識別子が分からない**ので、存在しない系列の版を握ることがない | 02 API設計書 2.0 |
| 就業規則の改定 | **現行版を閉じてから新版を入れるまでを 1 つの操作にする**（`WorkRuleRepository.revise`）。`save` を 2 回呼ぶと JPA が INSERT を先に流し、排他制約に弾かれる（落とし穴 157）| 02 API設計書 2.2 |
| 就業規則の新規登録と締め | **締め済みの月を拒まない。** 新しい系列はまだ誰にも適用されていないので確定済みの勤怠を動かさない。拒むのは適用の側。ここで拒むと過去に遡って規則を整備できなくなる | 02 API設計書 2.0 |
| 割増賃金の基礎額の分母 | **「所定労働日数 × 1 日の所定」で求めない。** 就業規則は年度の途中で改定できるので、この式は「1 日の所定が年度を通じて一定」を暗黙に仮定している。**所定労働日ごとに、その日に有効な版の所定を足す** | 07 ドメインモデル設計書 4.1 |
| 分母に使う就業規則 | **年度に実際に適用されている系列から集める。** 全系列を舐めると、廃止済みの系列や 8 年前の版まで巻き込み、一度でも違う所定の規則を作った会社は永久に出力できない。**系列のあいだで値が割れていたら 422** | 07 ドメインモデル設計書 4.2 |
| カレンダー未登録の判定 | **「年度の全日が登録されていること」。** 「法定休日が 1 日も無い」では、4 月だけ登録した年度がすり抜けて単価が 27% 低く出る。**全日登録を要求する以上、一括登録の手段も用意する**（落とし穴 94）| 07 ドメインモデル設計書 4.3 |
| 年間の所定の上限 | **法定の総枠（労基法 32 条）と比べる。ただし週の数は切り上げる。** 年度は週の整数倍ではない（365 日 = 52 週 + 1 日）ので、切り捨てると土日休みの正当なカレンダーを拒否する（落とし穴 23・51）| 07 ドメインモデル設計書 4.4 |
| 出力済みの年度のカレンダー | **変えられない**（`fiscal-year-used-by-payroll`・409）。分母は年度全体から決まるので、未締めの月を変えても既払いの割増賃金の単価が事後的に足りなくなる。ポートは `shared` に置く | 07 ドメインモデル設計書 4.5 |

詳細は [アーキテクチャ設計書](doc/02_詳細設計/00_共通/アーキテクチャ設計書.md)。

---

## 3. ディレクトリ構成

```
.
├── CLAUDE.md               このファイル
├── doc/                    設計文書（工程ごとに番号付き）
│   ├── README.md           文書の読み方
│   ├── _tools/             図・テスト一覧・マイグレーションの生成ツール
│   ├── 00_開発環境構築手順.md
│   ├── 01_要件定義/
│   ├── 02_詳細設計/        00_共通 と業務コンテキストごとに細分化
│   ├── 03_単体テスト/
│   ├── 04_結合テスト/
│   ├── 05_ADR/             設計判断の記録
│   └── 06_レビュー記録/    レビューの指摘と対応
├── src/
│   ├── backend/            Java (Spring Boot)
│   └── frontend/           React + TypeScript (Vite)。e2e/ に実ブラウザの通し
├── docker-compose.yml
├── .editorconfig
└── .gitattributes
```

---

## 4. 規約

### 4.1 ドキュメント

| 規約 | 内容 |
| --- | --- |
| 形式 | Markdown |
| **図** | **Mermaid のソース（`diagrams/*.mmd`）を正とし、PNG を生成して埋め込む。** 開いた瞬間に図が見える状態を保つ |
| 図の生成 | `cd doc/_tools && npm run render`。**PNG を直接編集しない** |
| **テストケース一覧** | **詳細設計書を正とし、`cd doc/_tools && python3 collect-tests.py` で生成する。一覧を直接編集しない** |
| **マイグレーション** | **DB設計書を正とし、`cd doc/_tools && python3 build-migrations.py` で生成する。`db/migration/*.sql` を直接編集しない** |
| 粒度 | 業務コンテキスト単位に細分化する。1 文書が長大にならないようにする |
| ID | 要件は `BR-xx`、アーキテクチャ規約は `AR-xx`、単体テストは `UT-xx`、結合テストは `IT-xx`。**ID はシステム全体で一意**（コンテキストごとに振り直さない） |
| 追跡可能性 | 要件 → 設計 → テスト → 実装 を ID で辿れる状態を保つ |

### 4.2 コミットメッセージ

```
1 行目に何をしたかを日本語で簡潔に

3 行目以降に、なぜそうしたかを書く。
何をしたかは差分を見れば分かるが、理由は書かないと残らない。
```

### 4.3 コード

| 規約 | 内容 |
| --- | --- |
| インデント | Java は 4 スペース、その他は 2 スペース（`.editorconfig` に定義） |
| 文字コード / 改行 | UTF-8 / LF（`.bat` のみ CRLF） |
| ドメインのクラス | 不変。`record` を第一選択とする |
| 種類が固定された概念 | `sealed interface` で表現し、`switch` の網羅性検査を効かせる |
| 不変条件 | compact constructor で検証し、不正なインスタンスを存在させない |
| JPA エンティティ | パッケージプライベート。`infrastructure` の外に出さない |
| 依存の注入 | コンストラクタインジェクション。フィールドインジェクション禁止 |
| 現在時刻 | `Clock` を DI する。`LocalDateTime.now()` を直接呼ばない |

### 4.4 Git

- 開発ブランチ: `claude/java-personal-project-ideas-f08w42`
- **履歴の書き換え（force push）は行わない。** 複数環境から更新するため
- `gradlew` の実行権限に注意（Windows でコミットすると落ちる）

---

## 5. 既知の落とし穴

過去に踏んだ、または踏みかけた問題。**同じ轍を踏まないこと。**

| # | 内容 |
| --- | --- |
| 1 | **`hibernate.jdbc.time_zone` を設定しない。** JDBC 層が `time` 型のカラムまで変換し、深夜帯 22:00–05:00 が別の時刻として読み込まれる |
| 2 | **Spring Boot 4 でスターター名が変わっている。** `spring-boot-starter-web` → `spring-boot-starter-webmvc`、Flyway は `spring-boot-starter-flyway`、テストは機能ごとに `-data-jpa-test` などへ分割。3.x 向けの記事をそのまま写さない |
| 28 | **Spring Boot 4.1 が管理する Testcontainers は 2.0 系だが、`junit-jupiter` / `postgresql` の座標が 1.x から変わっており解決できない。** BOM を 1.21.3 に固定する |
| 29 | **ArchUnit のルールのフィールド名に日本語を使わない。** フィールド名がそのままレポートのファイル名になり、locale が POSIX / C の環境（CI のコンテナ）で「Malformed input」でレポート生成が落ちる。`sun.jnu.encoding` は `-D` で上書きできない。説明は `because(...)` に書く |
| 30 | **ArchUnit の `noClasses()` は、対象が 0 件だと「1 件も検査しなかった」として落ちる。** 実装が進んでいない段階では `allowEmptyShould(true)` を付ける |
| 31 | **ArchUnit のルールは、わざと違反するクラスを置いて落ちることを確かめる。** 対象が無くて通っているのか、守れていて通っているのかを区別する |
| 3 | **`.gitignore` でスラッシュを含むパターンはその `.gitignore` の階層が基準。** `!gradle/wrapper/gradle-wrapper.jar` はサブディレクトリのプロジェクトに効かない。`!**/gradle/...` と書く |
| 4 | **`./gradlew build` はテストを含む。** データソース未設定の段階では `contextLoads()` が失敗する。生成直後の確認は `compileJava` で行う |
| 5 | **深夜は他の区分と重複する属性であり、労働時間を分割する区分ではない。** 集計時に「深夜のみが付いた所定内の区間」を数え損ねやすい。内訳の合計＝実労働時間、を不変条件として検証すること |
| 6 | **統合テストで H2 を使わない。** `EXCLUDE` 制約・パーティション・配列型が再現できず、通っても意味がない |
| 7 | **`EXCLUDE USING gist` には `CREATE EXTENSION btree_gist` が必須。** uuid の等値比較と範囲型の重なり比較を同居させるため。Flyway の初回マイグレーションの先頭に置く |
| 8 | **`CHECK` 制約に副問い合わせは書けない。** 配列の要素数を数えるような検証は `CASE WHEN ... = ANY(...)` の加算で書く |
| 9 | **`BEFORE` トリガは `CHECK` 制約より先に評価される。** どちらでも防げる不正データは、トリガのメッセージが返る |
| 10 | **閉区間と半開区間を混在させない。** 在籍期間（閉区間の感覚）と所属期間（半開区間）が混ざると、退職日当日の 1 日が消える |
| 11 | **自分で作った検証データを疑う。** 承認者不在の欠陥は、自分の検証結果に現れていたのに見逃していた |
| 12 | **検証では入力を 1 つだけ変える。** 2 つ変えると何を証明したのか分からなくなる |
| 13 | **有効期間を持つマスタを、参照側が「版」で指さない。** 改定した瞬間に全参照が切れる。系列を指させる |
| 14 | **同じ UUID でも意味が違うなら型を分ける。** `WorkRuleId` と `WorkRuleSeriesId` が同じ型だと取り違えてもコンパイルが通る |
| 15 | **法定値には上限も置く。** 割増率の下限だけ守っても、法定労働時間を 12 時間にされたら割増の対象が消える |
| 16 | **成立しない CHECK 条件を書かない。** 所定 ≤ 480 分を別の制約で保証しているなら「480 分超なら休憩 60 分」は永遠に成立しない |
| 17 | **検証が「拒否された」ことだけを見ない。** 期待した制約で拒否されたかを確認する。別の制約に先に引っかかると、狙った検証は行われていない |
| 18 | **同じ指摘が 2 回出たら、それは規約になっていない。** 個別に直さず [設計規約チェックリスト](doc/02_詳細設計/00_共通/設計規約チェックリスト.md) に項目を足す |
| 19 | **一次証拠の記録を、別の日の不整合や計算側の都合で止めない。** 打刻を拒否すると働いた事実が残らない |
| 20 | **勤務日と暦日は別のもの。** 労働時間の帰属は勤務日（BR-03）、法定休日労働の判定は暦日 |
| 21 | **採用する DB のバージョンと検証に使うバージョンをそろえる。** 検証できない版を設計書に書かない |
| 22 | **制度の分岐を暦日区分の分岐と混ぜない。** フレックスに日次の残業を付ける誤りは、`switch (dayType)` だけで書いたことから生まれた |
| 23 | **排他を主張する CHECK は、両方が正になる正当なケースが無いか先に探す。** 見落とすと適法なデータを保存できなくなる |
| 24 | **正常系の検証を 1 行にまとめない。** 「正常な FIXED / FLEX」を 1 行にしたことで、所定 > 総枠 の月の欠陥を見逃した |
| 25 | **「拒否された」ではなく「狙った制約で拒否された」ことを確認する。** 別の制約に先に引っかかると、狙った検証は行われていない |
| 26 | **実行者が不在になる経路を洗い出す。** 退職・部署廃止で提出・承認・締めが誰にもできなくなる状態を 2 回作った |
| 27 | **副作用として自動で起きる状態遷移も型にする。** そうしないと監査証跡に残せない |
| 32 | **秒の丸めを区間ごとに独立して適用しない。** 休憩開始を切り上げ・休憩終了を切り捨てると、1 分未満の休憩では直前の区間の終了が次の区間の開始より後になり、**区間が重なって労働時間が二重に計上される。** 乱数 20,000 件のうち 10,222 件が該当した。丸めた開始が直前の終了より前なら押し戻す |
| 33 | **拘束時間を実労働区間から求めない。** 出勤直後や退勤直前の休憩で区間が長さ 0 になって捨てられ、出退勤の時刻が失われる。出勤・退勤の打刻から直接求める |
| 34 | **`record` の `equals` は `BigDecimal` の<ruby>スケール<rt>小数の桁数</rt></ruby>も比較する。** DB の `NUMERIC(4,3)` は `0.250` を返すので、正規化しないと定数 `0.25` と等しくならず、読み書きを往復しただけで別の値になる |
| 35 | **`LocalDate.MAX` は PostgreSQL の `date` に入らない**（上限は 5874897 年）。番兵として使うなら、`infrastructure` が `NULL` に写す責務を明示し、日数計算は例外にする。素通りさせると 365241759927 日という値が黙って返る |
| 36 | **compact constructor が強制している不変条件を、その型のインスタンスに対して確かめない。** 破れていれば生成の時点で例外になるので、アサーションは**恒真**である。強制している当の場所に破れた値を渡す |
| 37 | **テストの代役（テストダブル）に業務ロジックを持たせない。** 日数の数え方を代役側に置くと、そのテストは本番のコードを 1 行も検査しない。既定実装をインタフェースに置き、代役は事実だけを答える |
| 38 | **実装を直したら、根拠になった設計書の節も同じコミットで直す。** 秒の丸めの欠陥を修正したとき Java だけを変え、設計書は「逆転はしない」と書いたまま残った。**修正済みの欠陥を設計書がなお正しいと主張する**状態になる |
| 39 | **導出できる値を列として持つなら、食い違いを DB で禁じる。** `calendar_date` は開始時刻から導けるのに、無関係な値を保存できた。集計は開始時刻で、法定休日の判定は `calendar_date` で行われて結果が食い違う |
| 40 | **長さ 0 の区間を作れない型に、長さ 0 になりうる値を渡さない。** 出勤と退勤が同一時刻だと拘束時間の生成が例外になり、`DomainException` ではないので 500 になって**打刻の登録そのものが落ちた** |
| 41 | **同時刻の打刻に固定の全順序を与えない。** 「休憩終了 → 休憩開始」（休憩を 2 つに割った境界）と「休憩開始 → 休憩終了」（長さ 0 の休憩）の両方が正当であり、一方にそろえると他方を拒否する。状態機械が受け入れられる打刻を先に採る |
| 42 | **「誰の」を引数で受け取らない解決関数を作らない。** `resolve(assignments, versions, date)` は他人の適用を渡されると**他人の規則を黙って返し**、複数人ぶんを渡されると「制約が壊れた」と誤診断した |
| 43 | **ミューテーションテストは実際に走らせる。** 「このテストがあるから守れている」は思い込みで、16 件試して多くが生存した。とくに**閾値をまたがない数字**を選んだテストは、ルールを消しても落ちない（落とし穴 24 と同型） |
| 44 | **PNG は再生成しても毎回バイト差分が出るものがある**（23 枚中 5 枚）。「`npm run render` して差分が出ないこと」で最新性を判定できない。`.mmd` を変えたときに再生成する運用で担保する |
| 45 | **生成器が拾えない ID を書かない。** `UT-WR-12b` のような接尾辞つき ID は `collect-tests.py` の正規表現に合わず、**黙って一覧から落ちる**。コードにしかない ID は追跡可能性の穴になる |
| 46 | **Spring Boot 4 は Jackson 3 を使う。** `ObjectMapper` のパッケージが `com.fasterxml.jackson.databind` から **`tools.jackson.databind`** へ変わっている（注釈は `com.fasterxml.jackson.annotation` のまま） |
| 47 | **`HttpStatus.UNPROCESSABLE_ENTITY` は非推奨。** RFC 9110 で 422 の名前が Unprocessable Content に変わった。`UNPROCESSABLE_CONTENT` を使う。**`-Xlint:deprecation` を有効にしていないと場所が分からない** |
| 48 | **Spring Data はクラスの内側に入れ子にしたインタフェースを走査しない。** リポジトリを 1 ファイルにまとめると Bean が作られず、コンテキストの起動ごと失敗する |
| 49 | **勤務日を打刻した暦日で決めない**（BR-03）。日跨ぎ勤務の退勤が翌日の勤務として記録され、**出勤の無い日に退勤だけが残る**。開いている勤務日を探して、そこへ追記する |
| 50 | **例外に HTTP のステータスを持たせない。** ドメインはフレームワークを知らない（AR-01）。業務上の分類（`DomainErrorKind`）だけを持たせ、HTTP への対応づけは `presentation` の網羅性検査つき `switch` で行う。文字列の対応表で引くと、追加した例外が 500 で黙って漏れる |
| 51 | **不変条件を、それが成り立つ根拠ごと確かめずに制度をまたいで適用しない。** 「時間外と不足は同時に正にならない」はフレックス（両方を同じ対象労働時間から導く）でしか成り立たず、固定時間制（時間外は日次・週次の実績、不足は所定総との差）では**忙しい週に残業して別の週に欠勤しただけの正当な月**を保存できなくなる。落とし穴 23 の再発 |
| 52 | **限度時間（月 45 時間・年 360 時間）に休日労働を数えない。** 36 条 3 項・4 項の対象は時間外労働だけで、休日労働を含めるのは 6 項 2 号・3 号（単月 100 時間未満・複数月平均 80 時間以内）という**別の規制**である。混ぜると、時間外 44 時間 + 法定休日 8 時間という**適法な月に偽の警告**が立ち、逆に 6 項違反を「限度時間の超過」と誤って伝える |
| 53 | **清算期間を通算する式を固定時間制に当てない。** 所定は 1 日ごとに約束されているので、19 日 8 時間・1 日 12 時間・1 日欠勤の月に「所定総 − 実労働」を当てると不足が 8 時間ではなく 4 時間になり、**残業が欠勤控除を相殺する。** 同じ 4 時間が「割増の対象」と「欠勤の穴埋め」に二重に使われる |
| 54 | **週や月をまたぐ集計を「末日が属する月」へ寄せない。** その月の清算が行われるとは限らない。月末が金曜の月に退職すると、最終週の週 40 時間超が存在しない翌月に割り当てられ**誰にも計上されない**。発生した暦日で振り分ける |
| 55 | **テストダブルが本番の分類を写経していないか確かめる。** 「本番の計算を通して作る」と javadoc に書きながら実際は通していない、という状態を作っていた。閾値の分割を代役側に持つと、日次と月次の継ぎ目を 1 行も検査しないテストになる（落とし穴 37 の再発） |
| 56 | **1 区間 177 時間のような、本番では決して現れない日次をテストで作らない。** 暦日境界も深夜帯もまたがないので、境界の処理を一切通らないまま「通った」ことになる。代役に上限を持たせて作れなくする |
| 57 | **楽観ロックの版を 0 から始めない。** `monthly_attendances` の行は提出時に初めて作られるので「行が無い月」と「作られたばかりの月」があり、どちらも版 0 だと**区別できない。** 提出前の画面（版 0）を握った承認者の要求が、提出後もそのまま一致して**楽観ロックが素通りする。** 行の作成時に 1 を入れ、0 は「行が無い」ことだけを指す値にする |
| 58 | **集約が持つ不変条件を、アプリケーション層を通してだけ検査しない。** 自己承認の禁止は `ApproverPolicy` の承認者判定で先に弾かれるため、**API のテストからはこの検査が一度も働かない。** 実際に禁止を削除しても API のテストは 1 件も落ちなかった。集約に直接あてるテストを書く |
| 59 | **同じクラスの中から `@Transactional` のメソッドを呼ばない。** Spring のプロキシを通らないので**トランザクションが始まらず**、一括締めが 1 つのトランザクションになる。1 件の失敗で 99 件の締めが巻き戻る。別のクラス（`BulkClosureService`）に分ける |
| 60 | **「一括」の失敗を 1 種類にしない。** 社員ごとの事情（未提出・未承認）は結果の `skipped` に、依頼そのものの不備（人事でない・対象月が終わっていない）は例外に分ける。全員を `skipped` にすると、人事は**自分に権限が無いことに気づけない** |
| 61 | **設計書がコンパイル時に保証すると書いた不変条件を、実装が実行時の検査にしていることがある。** 締め済みからの遷移は「型として存在しない」と書いていたが、遷移メソッドを状態ではなく集約に置いた（自己承認の判定に対象社員の ID が要る）ため `closed.reject()` はコンパイルが通る。**設計書が「テストは書けない」と言っている観点こそ、実装を確かめる** |
| 62 | **API のテストを設計書の ID に登録し忘れる。** `IT-APV-30`〜`47` はテストコードにしか無く、一覧から漏れていた。要件 → 設計 → テスト → 実装 の追跡が切れる（落とし穴 45 と同型）|
| 63 | **テストの前提が、狙った分岐をまたいでいるか確かめる。** 「退職者も一括締めの対象に含める」検査で退職日を 4/30（＝月末時点でまだ在籍）にしたため、在籍者だけに絞る変異が生き残った。月中退職（4/15）にして初めて検査になった（落とし穴 24・43 と同型）|
| 64 | **同じ値を 2 か所に持たせない。** 訂正の `Revoke` に `workDate` を持たせる設計だったが、申請そのものも `workDate` を持つ。食い違ったときにどちらが正しいか決められないので、申請の側にだけ置く |
| 65 | **打刻に識別子を持たせない。** 労働時間の計算に要るのは種別と時刻だけで、識別子を持たせると値としての等価性（同じ時刻の同じ種別は同じもの）が壊れる。識別子が要るのは訂正だけなので `RecordedTimeClockEvent` で外から添える |
| 66 | **DB の外部キー違反を業務エラーの代わりにしない。** 実在しない打刻を取消対象にした申請は `correction_items_target_fk` でも弾かれるが、**外部キー違反は利用者に説明できない。** 申請の時点で確かめ、どの打刻が見つからないかを返す |
| 67 | **再計算の手順を 2 か所に書かない。** 訂正の承認は「打刻から日次を作る」を必要とするが、これは打刻の登録がすでに持っている。`TimeClockService.recalculate` として 1 つだけ置き、訂正側から呼ぶ。写すと丸めや勤務日の扱いを直したときに片方だけが古くなる |
| 68 | **開いている勤務日に、出勤打刻まで吸わせない。** 前日の退勤を打ち忘れると、開いた勤務日へ翌朝の出勤が追記されて状態機械が壊れ、**その社員は翌朝そもそも打刻できなくなる。** 出勤は必ず新しい勤務日を始め、打ち忘れた日は警告として返す（落とし穴 19 の再発） |
| 69 | **状態機械の判定を SQL に写さない。** 「未退勤の勤務日」を `HAVING sum(CLOCK_OUT) = 0` で書くと、`isClosed()` と 2 か所に分かれる。SQL は候補を返すところまでにとどめ、判定はドメインに任せる（落とし穴 13・時点解決と同型） |
| 70 | **提出の事前条件を暦月で見ない。** 在籍していない日（入社前・退職後）の勤務日まで「日次勤怠が未確定」と数えると、その日に働くことはありえないので確定させる手段が無く、**月中入社の初月と月中退職の最終月を永久に提出できない。** 在籍期間との重なりで絞る |
| 71 | **設計書が「提出処理の内部で行う」と書いた再計算を省かない。** 提出時に月次清算を計算し直していなかったため、承認者は日次だけが直った古い月次を見て承認することになっていた。単体でも API のテストでも捕まらず、通しのシナリオで初めて出た |
| 72 | **全社で共有する表の変更を、社員単位の締め判定で守らない。** 会社カレンダーは 1 つしか無いので、特定の社員が未締めでも他の社員が締めていれば変更してはいけない。`isClosedForAnyone` を分けて置く |
| 73 | **有効期間を持つ行の「変更」を、挿入だけで実装しない。** `work_rule_assignments` に入れるだけだと期間が重なって EXCLUDE 制約に弾かれ、**一度適用した社員の就業規則を二度と変更できない。** 現行を閉じてから入れるまでを 1 つの操作にする（就業規則の改定と同型） |
| 74 | **落とし穴 29 は Java の型名とテストメソッド名にも当てはまる。** 日本語にすると `.class` とレポート HTML のファイル名になり、POSIX / C ロケールで書き出しに失敗する。業務の言葉は `@DisplayName` とヘルパのメソッド名に置く |
| 75 | **「その日に在籍していたか」と「名簿に載せるか」を 1 つのメソッドにまとめない。** 前者は入社日で絞るので**未来日入社の社員が一覧に現れず**、管理者が登録の成否を確認できない。名簿は退職者を除くかどうかだけで絞る |
| 76 | **`@JsonInclude(NON_NULL)` を record 全体に付けない。** 省きたいのが 1 項目でも、`department` や `retiredOn` の `null` まで消え、**「所属が無い」ことを応答から読み取れなくなる。** 項目ごとに付ける |
| 77 | **正規化を型が持っているなら、比較する側で畳み直さない。** `Email` は生成時に小文字化しているのに、重複検査でも `equalsIgnoreCase` を使っていた。同じ規則が 2 か所にあると変異が等価になり、**正規化を消してもテストが落ちない場所**が生まれる |
| 78 | **退職で部署長を閉じ忘れない。** 所属だけ閉じても、**その部署に所属する全社員の承認者が退職者になり続ける。** 既存の行が後から不正になる種類の問題なので、DB の制約では検出できない（落とし穴 26 と同型） |
| 79 | **期間を「開き直す」ときは、閉じた日で絞る。** 社員 ID だけで開き直すと、異動で閉じた過去の所属まで開いてしまい、期間が重なって排他制約に弾かれる。退職の取消が巻き戻すのは、退職で閉じたぶんだけ |
| 80 | **`Stream.findFirst()` は先頭が null だと NPE を投げる。** 「開いている期間」を表すのがまさに `valid_to IS NULL` なので、`queryForList(...).stream().findFirst()` で取り出すと落ちる。添字で取り出す |
| 81 | **PL/pgSQL の `RAISE` は制約違反ではない。** 循環検出トリガの拒否は `DataIntegrityViolationException` にならず Problem Details へ写らないので、**親の選び間違いが 500 になる。** 業務エラーは `application` 層で検出し、トリガは最後の防波堤として残す |
| 82 | **テストの接続プールを既定のままにしない。** `@TestBean` で時計を差し替えるたびに別のコンテキストが作られ、そのそれぞれが 10 本の接続を握る。10 個ほど積み上がると `max_connections` に届き、**後から起動したコンテキストが「too many clients」で丸ごと落ちる。** コード側の回帰と見分けがつかない |
| 83 | **親だけを廃止しない。** 配下に現存する部署が残ると、子が親のいない部署になり、承認者の遡りが根へ到達できなくなる（落とし穴 26 と同型） |
| 84 | **禁止先が複数ある ArchUnit ルールは、禁止先ごとに違反クラスを置く。** AR-06 は `infrastructure` と `presentation` の 2 つを禁じているのに、踏んでいたのは前者だけだった。**禁止先から `presentation` を削っても自己検査が通る**状態で、ルールが半分死んでも気づけない（設計書 9 章がまさにこれを警告していた） |
| 85 | **ルールの javadoc がルール自体より強いことがある。** AR-06 の説明は「参照してよいのは domain だけ」と書いていたが、設計書も実装も `application` を認めている。**読んだ人が実装のほうを疑う**ので、説明は必ずルールに合わせる |
| 86 | **「禁止先ごとに違反クラスを置く」という規約は、書いてあるだけでは守られない。** 設計書に明記していたのに、実際は AR-06 の `presentation`・AR-03 の `presentation`・AR-10 の 3 コンテキストが踏まれておらず、**禁止先を削っても自己検査が通る**状態だった。禁止先を 1 つずつ削って落ちるかを変異で確かめる |
| 87 | **違反クラスを書けない禁止先を置かない。** AR-01 は `io.swagger..` を挙げていたが springdoc は依存に入っておらず、効いているか確かめようがない。**「守られている」という見かけだけが増える。** 採用するときに違反クラスと一緒に戻す |
| 88 | **生成器の「クエリか DDL か」の判定を、先頭のコメント行ごしに当てない。** `build-migrations.py` は ```` ```sql ```` ブロックの先頭が `SELECT` なら参照用と見なすが、`-- 候補：…` から始まる SELECT は判定をすり抜け、**プレースホルダ入りの SELECT がマイグレーションに紛れ込んだ。** 生成し直すまで誰も気づかない |
| 89 | **生成器の警告は見過ごされる。落として気づかせる。** `UT-ATT-17`〜`23` が 2 つの観点に重複して振られ、`collect-tests.py` は毎回 7 件の警告を出していたのに、そのまま数か月残っていた。ID の重複は終了コード 1 にする（落とし穴 45・62 と同型） |
| 90 | **「確定しているか」を「行があるか」で判定しない。** 日次勤怠の行は**打刻がそろった日にしか作られない**ので、「在籍中の所定労働日すべてに行があること」は「全日出勤していること」と同義である。**1 日でも欠勤すると、その月を永久に提出できなかった。** 欠勤の日を確定させる手段は無く、打刻を足せば働いていない日の労働時間が一次証拠として残る。確かめるのは**「打刻があるのに日次勤怠が無い日」**（未退勤・就業規則の未設定）である（落とし穴 19・70 と同型） |
| 91 | **`a + n年 > b` を `a > b - n年` へ移項しない。** 日付演算にはクランプがあるので等価にならない。2024-02-29 に付与した年休の有効期間は、ドメイン（`plusYears(2)`）では `[2024-02-29, 2026-02-28)` だが、SQL の `granted_on > asOf - interval '2 years'` では 2026-02-28 も有効になる。**うるう年の 1 日だけ、残日数には出るのに申請すると拒否される。** 時点解決を SQL に写さない（落とし穴 13 と同型） |
| 92 | **「0 件なら満たしたものとする」という緩和を、対象の絞り込みと組で確かめる。** 出勤率は「全労働日 0 なら 8 割を満たす」としたが、**退職者は在籍期間で絞ると必ず全労働日 0 になる。** 付与の対象から退職者を外していなかったので、退職者に毎年 20 日が付与され続ける状態だった。緩和は必ず「誰に当てるか」とセットで書く |
| 93 | **状態ごとに違う期限を、1 つの述語にまとめない。** 年休の取消を `today.isBefore(leaveDate)` だけで判定すると、**承認されていない申請まで取得日を過ぎた瞬間に取り下げられなくなる。** 承認・却下も締め済みで拒否されるので、その申請はどの状態にも遷移できず、部分一意インデックスがその日を占有し続ける（落とし穴 26 と同型） |
| 94 | **「別の機能で扱う」と書くとき、その機能が実際に扱えるか確かめる。** BR-16 は取得日の当日以降の年休の取消を訂正申請（BR-09）へ委ねていたが、**訂正申請が動かせるのは打刻だけ**で年休の状態は動かせない。委譲先を確かめずに書いたので、取り消す手段がどこにも無かった |
| 95 | **「対称に行う」と書いた副作用が、どの遷移にも当てはまるか確かめる。** 年休の取消は配分を外し月次清算を計算し直し提出済みの月を下書きへ戻す、と書いたが、**承認されていない申請の取下げでは何も変わっていない。** そのまま実装すると、取り下げただけで提出済みの月が下書きへ戻り、`REVERT_BY_LEAVE` という嘘の証跡が残る |
| 96 | **表示する値と受理を判定する値を、別の式で求めない。** `availableDays`（あと何日申請できるか）を到来済みの付与だけで数え、申請の受理判定には到来予定の付与を含めていた。**表示のほうが少なく出る**ので、社員が権利を行使しない方向へ倒れる。同じ関数から導く |
| 97 | **一方を緩めたら、その先に生まれる経路を追う。** 「未決裁のまま取得日を過ぎた申請は取り下げられる」と決めた結果、**すでに働いた日の年休を後から承認できる**経路が新しく開いた。所定総からその日が除かれるのに実労働もあるので、不足時間が過少に出る一方で社員は年休を 1 日失う |
| 98 | **ある表の列を別のコンテキストで足したら、その表を所有する設計書も直す。** `monthly_settlements.paid_leave_days` を 06 で足したが、この表の列と不変条件を列挙しているのは 04 である。04 だけを見て実装する人にはその列が存在しない（落とし穴 38 と同型） |
| 99 | **未適用のマイグレーションを直したら、テスト用の DB を作り直す。** 設計書から生成し直した SQL は中身が変わるので Flyway のチェックサムが合わず、**全テストがコンテキストの読み込みで落ちる**（一度に 332 件）。原因はスタックトレースの奥にしか出ず、**コード側の回帰と見分けがつかない**（落とし穴 82 と同型）。`./doc/_tools/start-postgres.sh` が `kintai_test` を作り直す |
| 100 | **「廃止した ID」を表に書かない。** `collect-tests.py` は `\| ID \| … \|` の行を現役のケースとして拾うので、**廃止したはずの ID が一覧に復活する。** 箇条書きにする |
| 101 | **UT と IT で同じ番号を別の観点に振らない。** 文字列としては別の ID なので生成器は重複を検出しないが、`UT-LV-63` と `IT-LV-63` が無関係な観点を指していると、設計書とコードを突き合わせる人が取り違える。番号は通しで使う |
| 102 | **一括処理の結果を、日付や状態だけで絞って検査しない。** 付与の結果には対象の社員だけでなく、テストの前提として作った人事や上長の行も並ぶ。「その付与日の `Granted` は 1 件」と書くと、入社年の違う別の社員の付与が混ざって落ちる。**社員でも絞る**（落とし穴 12 と同型で、検査の対象を 1 つに定める）|
| 103 | **表示の絞り方と、受理判定の絞り方を別の軸にしない。** `availableDays` を「基準日に有効な付与」で数えたが、申請の受理判定は「取得日に有効な付与」で行う。**付与日がまだ到来していない付与だけが表示から落ち**、0 日と出るのに申請は通った。落とし穴 96 は「同じ関数から導く」と書いていたが、同じ関数でも**渡す日付が違えば別の式である。** しかもテストは「付与日は到来済みで行が無い」場合を選んでいて、境界の内側にいた（落とし穴 24 の再発）|
| 104 | **一括処理のループを `try`/`catch` 無しで回さない。** 1 人で例外が出ると残りの社員が一件も処理されず、結果が返らないので**その日の付与が途中で止まったことに誰も気づけない。** `REQUIRES_NEW` は巻き戻りを隔離するだけで、ループの離脱は防がない（落とし穴 59 と 60 は別の問題）|
| 105 | **利用者が送る値の検証を、`record` の compact constructor に任せない。** 人事が申告する「出勤扱いの日数」は `IllegalArgumentException` で弾かれ、実装の不備として**理由の載らない 500** になっていた。設計書は 422 を求めていた。compact constructor は最後の防波堤であって、業務エラーの窓口ではない |
| 106 | **「この状態は起きない」という注釈が、別のコンテキストでも成り立つか確かめる。** `ApproverPolicy` は「対象月に所属が無い月には月次勤怠が存在しないので、提出も承認も起きない」として `NONE` を返していた。**年休は在籍していない月の取得日でも申請しうる**（未来日の申請のあとに退職が登録される）ので、承認も却下も取下げもできない申請が残った（落とし穴 94 と同型）|
| 107 | **一覧に無い ID がコードにだけあっても、生成器は設計書しか見ないので気づけない。** 廃止した `UT-LV-46` がテストに残り、同じ ID を 2 つの観点が名乗っていた。`collect-tests.py` に**テストコードとの突き合わせ**を足して落とす。ただし**同じクラスの中の複数のテストは落とさない** — 1 つの観点を閾値の両側から見るのはむしろ正しく（落とし穴 24）、落とすと「1 行にまとめる」方向へ圧力がかかる |
| 108 | **応答の `type` を確かめないテストは、エラーの取り違えを検出しない。** 05 の設計書は承認者でない場合に `not-approver` を返すと書き、Problem Details の例まで載せていたのに、実装は一般の `forbidden` を返していた。**テストは `isForbidden()` しか見ていなかった**ので、設計と実装が食い違ったまま数か月動いていた（落とし穴 17・25 が HTTP に現れた形）|
| 109 | **一覧の期待を「関係」で書かない。** `IT-LV-32` は `remainingDays > availableDays` を期待と書いていたが、`availableDays` が未到来の付与を数えるようになった時点で成り立たなくなった。**観点は「未処理の申請が引かれる」ことなので、前後の差で書く。** 付随して成り立っていた関係を期待にすると、正しい変更で落ちる |
| 110 | **同じ業務ルールが、コンテキストごとに違うエラー型を返していないか確かめる。** 自己承認の禁止（BR-11 の 4）は、月次勤怠と訂正申請では `ApproverPolicy` の判定が先に走るので `not-approver` が返り、年休だけが `self-approval` を返していた。**集約の検査に任せると、承認者の判定が先に本人を弾く**ので働かない（落とし穴 58）。認可の検査そのものを「自己 → 承認者」の順に並べ、集約の検査は経路外のために残す |
| 111 | **副作用の分岐を、別の入口のテストで代用しない。** 「取り下げると所定総が戻る」を人事の取消（`revocation`）で書いていたので、本人の取下げ（`cancellation`）の副作用の分岐を 1 件も通らないまま両方が緑だった。同じ結果になる 2 つの入口があるなら、**それぞれの入口から 1 本ずつ**通す |
| 112 | **応答の項目を 1 つも見ていないテストは、その項目の変異を検出しない。** `remainingDaysUntilDeadline` は半開区間の上限まで数えていて 1 日多かったが、どのテストも読んでいなかった。設計書に応答例の数字がある項目は、その数字で確かめる |
| 113 | **「分離できない」と書く前に、分離が必要かを問う。** 04 は「深夜かつ 60 時間超（75%）を分離するには 60 時間到達の時点が要るが、週 40 時間超とフレックスの時間外が紐づかないので確定できない」と未決にしていた。しかし**深夜割増は上乗せ**なので、排他区分の時間と深夜の合計を別々に渡せば受け取った側が計算できる。**解けない問題を立てていただけで、問題そのものが不要だった** |
| 114 | **同じ文書の中で、過去に潰した欠陥を復活させない。** BR-18 の不足時間の出どころに「所定総 − 実労働」と書いたが、版 0.7 で BR-04 が**まさにこの式を固定時間制に当ててはならない**と反例つきで禁じていた。同じ要件定義書の中である。新しい節を書くときは、**その節が触れる既存の BR を読み直してから書く**（落とし穴 53 の再発）|
| 115 | **「割増率が同じ」と「支払う賃金が同じ」を混同しない。** 所定内労働と法定内残業はどちらも割増 0% だが、法定内残業には通常の賃金 1.0 倍の追加支払が要る。率だけを見て 1 列に束ねると、給与側は払う手段を失う。**率で分類する前に、支払義務で分類する** |
| 116 | **法が「記載しなければならない」と定めた項目を、導出できるからと省かない。** 労働日数（労基則 54 条）は打刻を持つ本システムにしか作れないのに、BR-18 の出力に無かった。実労働時間も「①〜④の合計」で導出させていた。**受け手が別の式を書く余地を残す**（落とし穴 39 と同型）|
| 117 | **合計が一致することを不変条件と呼ぶ前に、代入して恒真でないか確かめる。** 「①〜④の合計 = 実労働」は ① を `実労働 − ② − ③ − ④` として定義していたので、**定義を代入しただけ**だった。意味があるのは `① ≥ 0` のほうである（落とし穴 5・36 と同型）|
| 118 | **「別の機能が持っている値だから足りる」と考える前に、その値がその形で渡るか確かめる。** 割増賃金の基礎額に要る「1 年間における 1 か月平均所定労働時間数」（労基則 19 条 1 項 4 号）は会社カレンダーから出せるが、**1 社員 × 1 か月の行には入らない。** 出力の粒度が値の粒度と合っているかを見る |
| 119 | **2 つの分類が入れ子だと決めつけない。** 給与へ渡す値を「割増の付かない時間 → 所定内 + 法定内残業」と割ったが、**基礎賃金の要否（所定との比較）と割増の区分（37 条）は別の切り方**である。所定総が法定総枠を上回る月では**所定に届いていないのに時間外労働が生じる**ので、割ると基礎賃金を二重に払う。分類を足す前に、2 つの軸が交差する例を探す（落とし穴 51 と同型）|
| 120 | **「行が無い」ことに意味を与える前に、その表がいつ作られるかを読む。** 除外の理由に「月次勤怠の行が無い＝打刻が 1 件も無い」を置いたが、**行は提出時に初めて作られる**ので、行が無いことは「下書き」を意味する。1 か月まるまる働いて提出していないだけの社員に「打刻漏れを確かめよ」と案内していた。判定は一次証拠（打刻）の有無で行う（落とし穴 90・57 と同型）|
| 121 | **部分一意インデックスの「一意」を無条件の一意として使わない。** `employees_employee_number_uk` は `WHERE retired_on IS NULL` なので、**退職者の社員番号は再利用できる**。月中退職と同月の入社が重なると、給与 CSV に同じ社員番号の行が 2 つ並び、給与側が別人を同一人物として名寄せする。**同じ危険を自分で認識している場所がある**（`exported_by` を ID にした理由）のに、鍵には当てなかった |
| 122 | **「値が動かない」と「集合が動かない」を混同しない。** 締め済みの月次清算は動かないので「何度出しても同じ CSV」と書いたが、**出力の記録を作ったあとに残りの社員を締めれば行が増える。** 記録の件数と CSV の行数が食い違い、記録が「何を出したか」を指さなくなる。対象社員を行として残して集合を固定する |
| 123 | **既定値が「安全な側」かを、その値を使う式ごとに確かめる。** 会社カレンダーは登録漏れを所定労働日として扱う（休日と誤ると休日割増が付いて過払いになるため）。ところが**割増賃金の基礎額の分母**にこの既定を通すと、未登録の年度が 365 日になり月平均が 1.5 倍、**単価が法定を約 33% 下回る。** 同じ既定値が、ある式では安全側、別の式では危険側に倒れる |
| 124 | **同じ業務ルールに、コンテキストごとに違う HTTP を割り当てない。** 「対象月の末日が到来していない」は 05 が 409（`CONFLICT`）と決め実装もそうなっているのに、07 の設計書では 422 と書いていた。エラー型を新設する前に、同じ条件が既にどこかで定義されていないか探す（落とし穴 110 が HTTP に現れた形）|
| 125 | **「A × B」で求める式が、A と B の一方が期間中に変わらないことを仮定していないか確かめる。** 割増賃金の基礎額の分母を「所定労働日数 × 1 日の所定」で求めていたが、就業規則は年度の途中で改定できる。10 月に 7 時間 45 分へ改定された年度で、黙って過大な分母を返す。**日ごとに足せば仮定が消える** |
| 126 | **要件が禁じているだけの前提の上に、静かに 1 つの値を返す API を作らない。** 要件 3.1 は全社員 1 日 8 時間と定めているが、**DB は所定を「法定 8 時間を上限とする 1 分単位の値」として持てる。** 7 時間 45 分の社員に 8 時間の分母を当てると単価が 6.6% 低くなる。**検査されていない前提は、守られているという見かけだけを増やす**（落とし穴 87 と同型）|
| 127 | **弱い条件を、強い条件の代わりに使わない。** カレンダー未登録の判定を「法定休日が 1 日も登録されていないこと」にしたが、**4 月だけ登録した年度は法定休日を 4 日持つので通過する**（単価が 27% 低く出る）。防ごうとした事故が、より起きやすい形ですり抜けていた。**欠けているものそのものを数える** |
| 128 | **年や月をまたぐ上限を、単位期間の上限から割り算で作らない。** 「年間の所定 ≤ 暦日数 ÷ 7 × 週 40 時間」と書いたところ、**土日を休みにしただけの正当なカレンダー**（261 日 × 8 時間 = 2,088 時間）が総枠 2,085 時間 42 分を超えて拒否された。年度は週の整数倍ではない（365 日 = 52 週 + 1 日）。**端数の週も 1 週と数える**（落とし穴 23・51 の再発）|
| 129 | **テストのメソッド名を日本語にしない**（落とし穴 74 の再確認）。シナリオを日本語のメソッド名で書いたところ、**テストは全部通ったのにレポートの書き出しが「Malformed input」で落ちてビルドが赤くなった。** 業務の言葉は `@DisplayName` に置く |
| 130 | **「締め済みを守っている」ことと「値が動かない」ことを混同しない。** 割増賃金の基礎額の分母は**年度全体**から決まるので、締め済みの月を守っても**同じ年度の未締めの月**を変えれば動く。4 月分を払ったあとに 12 月の休日を増やすと、既に払った割増賃金の単価が事後的に足りなくなる（37 条の割増は下限）|
| 131 | **「無いのが正常な期間」と「本当の隙間」を区別する。** 年度の所定を系列ごとに 1 年ぶん足す実装は、系列ごとに**年度の全所定労働日について版を要求する**。10 月からフレックスを導入した会社の新系列は 4〜9 月に版を持たないのが正常なのに、これを隙間と誤診断して**その年度のどの月も永久に出力できなくなる**。しかも設計書は「廃止済みの系列を巻き込むと永久に出力できない」ことを避けるために系列を絞り込んでおり、**別の検査で同じ結果へ戻していた**。判定を「日ごと」にすると、年度途中の改定も同じ式で正しく数えられる |
| 132 | **同じ名前の値が 2 つの用途を持っていないか、用途ごとに確かめる。** 「所定労働日数」は日額（月給 ÷ これ）の分母でもあり、所定総労働時間の根拠でもある。前者は**暦月**、後者は**清算期間**（暦月 ∩ 在籍期間）で数える。清算期間のほうだけを渡して日額の分母と指定していたので、月中入社の月の控除が **約 1.9 倍**（労基法 24 条の全額払い違反）になり、しかも**暦月の所定労働日数は他のどの列からも復元できなかった** |
| 133 | **「変更を止める」ではなく「壊れる変更を止める」と書く。** 出力の有無だけでカレンダーを凍結すると、(a) 法定休日 → 所定休日の付け替えのように**分母を動かさない訂正**まで年度いっぱい止まり、(b) 就業規則の適用のように**分母を動かす別の入力**は素通りする。しかも**人事が動作確認で 1 回叩いただけ**でその年度が凍結された（記録の削除 API は無い）。書き込んだあとに数え直して記録した値と突き合わせれば、3 つとも同時に直る |
| 134 | **引き算で導いた値に `Math.max(0, …)` を被せたら、そこは重なりを隠している。** 欠勤日数を `所定労働日数 − 年休 − 出勤` で求めていたが、**年休を承認した日に出勤した月**（落とし穴 97）ではその日が両方に数えられ、同じ日を 2 回引く。クランプは負を止めるだけで重なりを取り除かないので、**欠勤控除が 1 日ぶん静かに漏れる**。定義（どちらでもない日）をそのまま数える |
| 135 | **書き込みのあとに検査する規則は、書き込みのあとで呼ぶ。** 「出力に使った分母が動いていないか」は変更後の状態を数え直して比べるので、`calendar.save` より前に置くと何も検査しない。前もって「変更したらどうなるか」を組み立てると、数え方の実装が 2 か所になる（落とし穴 67）。例外が飛べばトランザクションごと巻き戻る |
| 136 | **端の 2 点だけを見る検査を、範囲の検査と呼ばない。** 一括カレンダー登録の年度ガードは `from` と末日の年度しか見ておらず、**3 年度にまたがる期間の中間年度が素通り**した。すぐ隣の `requireNoClosedMonth` は全月をループしていたので、**同じメソッドの中で守り方が非対称**だった。片方を直したときにもう片方に気づけない |
| 137 | **同じ検査を 2 か所に置いてある状態を「守られている」と読まない。** `PayrollExportService` から人事の検査を丸ごと削っても全テストが緑だった。POST が `annualScheduledHours` を呼び、その先にも同じ検査があるからである。**CSV 本体と一覧には下流の検査が無い**ので、そこは本当に空いていた。変異が生き残ったら、まず「別の検査が肩代わりしていないか」を見る |
| 138 | **設計書が呼んでいる API が実装されているか、書いた側とは別に確かめる。** 画面設計書は `availableActions` を返す `GET .../attendances/current` を前提に「状態機械を画面に複製してはならない」と書き、複製したコードを「誤り」として名指ししていた。ところが**その API は実装されていなかった**ので、画面を作れば必ずその誤りを書くことになる。月次勤怠の `canSubmit` も同じで、ドメインには判定があるのに presentation が外へ出していなかった。**設計書だけを読んだ人は、存在しない API に依存した画面を作る**（落とし穴 38・98 が層をまたいで現れた形）|
| 139 | **「依存に入っている」と「開いている」を混同しない。** actuator は最初から依存にあり `management.endpoints` で公開もしていたが、`anyRequest().denyAll()` が `/actuator/**` を塞いでいた。**コンテナの `HEALTHCHECK` は起動直後から永久に失敗する状態**だったのに、一度も叩かれたことが無いので誰も気づかなかった。設定を書いただけの口は、開いているように見えるだけである（落とし穴 87 が運用に現れた形）|
| 140 | **開発に便利な既定値を、本番のプロファイルへ引き継がない。** `${KINTAI_DB_URL:jdbc:postgresql://localhost:5432/kintai}` は開発では便利だが、本番では**環境変数を渡し忘れたコンテナが黙って localhost の開発 DB を探しに行く**ことを意味する。同居していれば繋がってしまう。起動しないほうが、誤った DB へ書き込むよりはるかに安全である（落とし穴 123 と同型）|
| 141 | **Spring MVC が投げる例外を既定のままにしない。** 必須パラメータの欠落も URL の綴りの誤りも、応答本文を持たないまま `/error` へ内部転送され、その転送を `anyRequest().denyAll()` が拒むので**本文の無い 403** として届く。「権限が無い」と「要求が間違っている」は利用者にとってまったく違う話であり、取り違えると原因に辿り着けない。**MockMvc は `/error` への転送を再現しない**ので、API のテストが 1000 件あっても 1 件も落ちない |
| 142 | **型の不一致を実装の不備として扱わない。** `UUID.fromString` も `LocalDate.parse` も `IllegalArgumentException` 系を投げるので、実装の不備を拾うハンドラが先に捕まえて**理由の載らない 500** になる。送った値が悪いことは利用者にしか直せない（落とし穴 105 が経路パラメータに現れた形）|
| 143 | **手書きの API の型は、コンパイラに守られていない。** TypeScript は「サーバがそう返すはず」という宣言を検査できない。`SignedIn` を `employeeId` と書いていたが応答は `id` であり、**型検査もテストも通ったまま `/api/employees/undefined/...` を呼ぶ画面**ができた。実物のバックエンドに当てて初めて出る |
| 144 | **読み込みに失敗したまま「読み込み中…」を出し続けない。** 失敗を捕まえて状態へ入れても、描画が「値が無ければ読み込み中」のままだと、利用者には永久に回り続ける画面としか見えない。**エラーの表示に辿り着けない** |
| 145 | **画面の節ごとに失敗を持つ。** `Promise.allSettled` の結果を握りつぶすと、日ごとの実績が取れなかった月に「計算済みの日がありません」と表示され、**失敗が成功として見える**（落とし穴 120 が画面に現れた形）|
| 146 | **テストの ID を振る前に、その番号が空いているか確かめる。** `IT-AUTH-13`・`IT-API-30` は既に別の観点が使っていた。**生成器が見ていない置き場所（フロントエンド）に書くと、重複しても誰も気づかない。** 生成器の走査対象を先に広げる（落とし穴 101・107 と同型）|
| 147 | **`gradlew test` を 2 つ同時に走らせない。** 同じ `kintai_test` を掴むので、デッドロックと制約違反が 69 件出る。**コード側の回帰と見分けがつかない**（落とし穴 82・99 と同型）。片方を強制終了すると Gradle の状態も壊れ、以後 `EOFException` で即座に落ちる。`build/test-results` を消して `--stop` してから流し直す |
| 148 | **検証のために本番コードへ改変を当てている間は `git add -A` を使わない。** 文書だけのつもりのコミットが変異を巻き込み、**まっさらなブラウザから誰もログインできない状態が HEAD に戻った。** コミットメッセージは差し戻しに触れないので履歴を読んでも気づけず、`git status` も clean と出る（HEAD 自体が変異を含むため）。エージェントに変異テストをさせている間はとくに危ない |
| 149 | **「等価変異だ」と結論する前に、その行が守ると言っている経路をすべて試す。** `dispatcherTypeMatchers(ERROR).permitAll()` は、例外ハンドラが拾える 3 経路では確かに等価だった（ハンドラがステータスを直接立てるので ERROR 転送に入らない）。しかし **`StrictHttpFirewall` の拒否と 406 は `DispatcherServlet` の手前と外で起きる**ので、この行が無いと本文の無い 403 になる。試した範囲が狭いと、生き残った変異を「消してよい」と誤って判断する（落とし穴 24 の変異版）|
| 150 | **`LocalDateTime` の `toString()` を API の書式に使わない。** 秒が 0 のとき秒そのものを省くので `09:00:00` が `09:00` になり、**桁数が値によって変わる。** 受け取る側は必ずどちらかで取り違える。書式は明示する |
| 151 | **画面が落ちない型の食い違いのほうが危ない。** 存在しない項目を読むと `undefined` になり、`Math.floor(undefined / 60)` は例外ではなく `NaN` を返す。**「時間外」列に `NaN:NaN` と出るだけ**で、画面は動き続ける。落ちてくれれば気づける（落とし穴 143 の、より気づきにくい形）|
| 152 | **一覧から開いた詳細の操作に、一覧側の絞り込み条件を使わない。** 承認画面は決裁の URL を画面の月セレクタから作っていたので、**詳細を開いたまま月を切り替えると、表示している月と違う月を承認**できた。操作の対象は開いている集約から取る |
| 153 | **通せる回数が 1 回だけのシナリオを書かない。** 打刻のシナリオは「未出勤から始まる」ことを前提にしていたので、2 回目の実行は「すでに退勤済み」から始まって落ちた。**製品の欠陥と見分けがつかない形で落ちる。** 前提データの投入を手順書に書くだけにせず、実行のたびに自動で入れ直す（`globalSetup`）。バックエンドが `@BeforeEach` で TRUNCATE するのと同じ理由である |
| 154 | **落とし穴の番号を、末尾を見ずに振らない。** 124 の直後へ 13 件を挿し込んだが、**その下にはすでに 125〜140 があった。** 同じ番号を 2 つの教訓が名乗り、参照（「落とし穴 131 と同型」）がどちらを指すか決められなくなる。**落とし穴 146 は、まさにこれをテストの ID について書いたものである**（自分で書いた規約を、その規約を書いた文書自身に当てていなかった）。表の一番下を見てから足す |
| 155 | **「テストが全部緑」を「実装が揃っている」と読まない。** テストは<strong>存在する API しか検査しない</strong>ので、設計書が定義していて実装が無い経路は 1000 件あっても 1 件も落ちない。実際 M4 の時点で 11 経路が欠けており、**就業規則の登録・改定はユースケース 1 本ぶんが丸ごと無かった**（DB へ直接 INSERT するしか作れず、e2e の seed が実際そうしていた）。設計書の一覧表と実装を突き合わせる検査を機械にやらせる（`check-endpoints.py`）|
| 156 | **突き合わせの検査を書いたら、壊して落ちることを確かめる。** `check-endpoints.py` は最初、**コメントアウトした `@GetMapping` を「実装がある」と数えていた**。1 行コメントアウトして試したら落ちなかったので気づいた。書いただけの検査は「守られている」という見かけだけを増やす（落とし穴 87 と同型）|
| 157 | **JPA は 1 回のフラッシュで INSERT を UPDATE より先に実行する。** 就業規則の改定で「現行版を閉じてから新版を入れる」つもりで `save` を 2 回呼ぶと、DB には**入れてから閉じた**順で届き、期間が重なって排他制約に弾かれる。順序は永続化の都合なので、閉じてから入れるまでを 1 つの操作（`revise`）にして永続化に閉じ込める（落とし穴 73 の再発）|
| 158 | **応答の版を手で書いた定数にしない。** 登録の応答に `version: 1` と書いたが、DB の `@Version` は 0 から始まっていた。**応答だけが嘘をつき、その値で改定すると必ず 409 になる。** 保存したあとに読み直して返す |
| 159 | **列を作っただけで楽観ロックが効いていると思わない。** `daily_attendances.version` は DDL にあるのに一度も読み書きされておらず、**どの再計算も版 0 のまま**だった。列の存在と、それを使う経路の存在は別である（落とし穴 139 が DB に現れた形）|

---

## 6. 現在地

| マイルストーン | 内容 | 状態 |
| --- | --- | --- |
| M0 | 開発環境の整備 | 完了 |
| 要件定義 | 要件定義書 | 完了 |
| 詳細設計 | アーキテクチャ / 各コンテキストの設計 | **完了**（00〜05・90_画面。レビュー 2 回・指摘 155 件を反映。DDL は PostgreSQL 16 で 104 件の制約検証を実施） |
| テスト設計 | 単体テスト仕様書 / 結合テスト仕様書 | **完了**（単体 126 件・結合 104 件 + シナリオ 12 件） |
| M1-a の土台 | Flyway / Testcontainers / ArchUnit / 制約テスト | **完了**（テスト 128 件が green。設計書の IT-ID 104 件を全数実装） |
| M1-a | マスタと固定時間制の日次計算 | **完了**（ドメイン・永続化アダプタ・API。テスト 407 件が green。Problem Details の変換、打刻の登録、日次勤怠の参照を MockMvc で API から DB まで通した） |
| M1-b | フレックスの月次清算、週 40 時間超、36 協定 | **完了**（ドメイン・永続化・結合。テスト 494 件が green。実装レビュー第 4 回の指摘 6 件を反映し、要件を 0.8 まで改訂） |
| M1-c | 認証・認可、打刻訂正、提出 → 承認 → 締め | **完了**（認証・認可、月次清算 API、提出 → 承認 → 締め、一括締め、楽観ロック、打刻の訂正申請） |
| M1-d | 積み残しの解消とシナリオ結合テスト | **完了**（`unclosedWorkDates`、カレンダー・規則適用の API と締めガード、シナリオ IT-SCN-01〜12。テスト 659 件が green。通しで初めて出た欠陥を 5 件修正） |
| M1-e | 社員・組織のマスタ保守 API | **完了**（社員名簿、異動・退職・ロール、部署ツリー。テスト 705 件が green。変異 33 件を確認） |
| M2/A | 年次有給休暇の要件定義（BR-14〜BR-17） | **完了**（要件を 1.0 へ改訂。比例付与と使用者による時季指定は対象外と決めた） |
| M2/B | 年次有給休暇の詳細設計 | **完了**（`doc/02_詳細設計/06_年次有給休暇/`。設計レビュー 2 回・指摘 47 件を反映。DDL は PostgreSQL 16 で 40 件の制約検証を実施） |
| M2/C-A | `leave` のドメインと単体テスト | **完了**（付与・残日数・申請の集約。テスト 771 件が green。AR-10 の禁止先に `leave` を足し、違反クラスを変異で確かめた） |
| M2/C-B | `leave` の永続化アダプタと制約テスト | **完了**（3 表のアダプタと `PaidLeaveDays`。IT-LV の制約 40 件と往復 13 件） |
| M2/C-C | 付与・申請のアプリケーション層と月次清算への接続 | **完了**（付与バッチ・申請/承認/取消・残日数の照会・提出時の警告。テスト 866 件が green。実装／テストの独立レビュー 2 本を回し、`availableDays` の欠陥ほか 14 件を修正）|
| M2/C-D | 年次有給休暇の API | **完了**（12 エンドポイント。IT-LV-31〜67・78〜98 と追加 15 件。実装／テストの独立レビュー 2 本を回し、指摘 20 件を反映。変異 11 件を確認。テスト 936 件が green）|
| M3-a/A | 給与連携の要件定義（BR-18） | **完了**（要件を 1.5 へ改訂。未決 4 件を解決。法令適合レビューの指摘を反映し、不足時間の式・①の粒度・賃金台帳の記載事項を直した）|
| M3-a/B | 給与連携の詳細設計 | **完了**（`doc/02_詳細設計/07_給与連携/`。新しいコンテキスト `payroll`。設計レビュー 2 本で指摘 29 件を反映し、要件を 1.6 へ改訂。DDL は PostgreSQL 16 で 9 件の制約検証を実施。UT-PAY 17 件・IT-PAY 34 件）|
| M3-d/A | 画面設計書が要求する API の穴 | **完了**（`availableActions`・月次の可否と履歴・打刻の一覧。設計書が呼んでいるのに実装が無かった 3 経路。IT-ATT-29〜35・IT-APV-82〜88・UT-ATT-40/41）|
| M3-a/C | 給与連携の実装 | **完了**（`payroll` コンテキスト・4 エンドポイント。前提として employee / approval / attendance / workrule に 12 件の経路を新設。実装／法令適合／テスト実効性の 3 エージェントで独立レビューし、指摘 20 件を反映。変異 24 件を確認（生き残った 4 件はテストを足して殺した）。UT-PAY 30 件・IT-PAY 54 件・IT-SCN 3 件。テスト 1037 件が green）|
| M3-b | シフト | **対象外**（[ADR 0008](doc/05_ADR/0008_シフト勤務を対象外とする.md)）。対象企業（要件 2.2）が固定時間制とフレックスの 2 種であり、3.1 の改訂は BR-04・BR-05・BR-14・BR-18 の 6 か所を同時に作り直す。とくに BR-18 の `multiple-scheduled-working-times` が常時発火して給与 CSV が出せなくなる |
| M3-c | コンテナ化とデプロイ | **一部完了**（actuator を Security から開けた・本番プロファイル・Dockerfile・本番 compose・構造化ログ・アクセスログ・認証の記録・運用手順書 4 本・ADR 0009。IT-OPS-01〜11）。**像のビルドと通しは未検証**（この環境からコンテナレジストリへ到達できない。[08_運用/README](doc/08_運用/README.md) に未検証の一覧を残した）|
| M4 | フロントエンドを縦に 1 本通す | **完了**（Vite + React 19 + TypeScript。SC-01〜03・05/06 を実物のバックエンドへ接続。UT-FE 12 件、実ブラウザの通し IT-SCN-32〜34。**ブラウザから通して初めて出た欠陥を 5 件修正**（ログインの 500・本文の無い 403・型不一致の 500・応答の項目名・永久に回る読み込み中）。独立レビュー 3 本（型の突き合わせ／セキュリティ／変異）で**さらに 11 件を修正**し、生き残った変異 1 件を IT-OPS-12 で殺した。[レビュー第 5 回](doc/06_レビュー記録/05_フロントエンド_第5回.md)）|
| M5 | 設計書にあって実装が無かった API を埋める | **完了**（就業規則の登録・改定・参照 6 経路、適用履歴、未適用一覧、カレンダー取得、36 協定の超過者一覧、日次の再計算。**設計 71 経路 / 実装 71 経路が一致**。`check-endpoints.py` で機械的に突き合わせ、壊して落ちることも確認。テスト 1105 件が green）|

---

## 7. 未決事項

| # | 内容 | 決定時期 |
| --- | --- | --- |
| 1 | 祝日データの取得方法（内閣府の CSV 取込か手動登録か） | M1-a |
| 2 | 複雑な集計クエリを JPQL にするかネイティブクエリにするか | DB 設計書 |
| 3 | ~~フロントエンドの API 型を手書きするか OpenAPI から生成するか~~ **解決済み。手書き。** 画面が実際に使う項目だけを書く。ただし**手書きの型はコンパイラに守られていない**（落とし穴 143）ので、実物のバックエンドへ当てる通しのシナリオを必ず置く | 完了 |
| 4 | 監査証跡をアプリケーションで記録するか DB トリガで記録するか | DB 設計書 |
| 5 | ~~給与計算システムへの連携形式~~ **解決済み。CSV**（BR-18）。時間と日数だけを渡し、金額は給与側が計算する | 完了 |
| 6 | ~~年次有給休暇の付与基準日~~ **解決済み。入社日基準**（BR-14）。一斉付与は初年度に法定どおりの前倒し付与が要り、結局「入社日基準ならいくらか」の計算を内部に持つことになる | 完了 |
| 7 | ~~パスワードの初期発行と再設定の運用~~ **解決済み。** 要件 BR-13 に定めた（`ADMIN` が発行・再設定し、口頭で伝える。メールでのリセットは設けない） | 完了 |
| 8 | ~~M1-a の時点では認証をかけていない~~ **解決済み。** M1-c/A で社員番号によるログインとロール認可を入れた。未認証は 401、CSRF は有効 | 完了 |
