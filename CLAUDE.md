# CLAUDE.md

このリポジトリで作業するときに、まず読むファイル。
**合意済みの決定事項を集約し、同じ議論を繰り返さないためにある。**

決定を変える場合は、このファイルを更新し、理由を ADR に残すこと。

---

## 1. プロジェクトの概要

架空企業「株式会社サンプル」（従業員 100 名・単一事業所）の勤怠管理システム。
個人開発であり、**ポートフォリオとしての提示** と **設計手法の習得** を目的とする。

主題は機能の多さではなく、次の 2 点である。

1. 業務ルールを型で表現し、ありえない状態をコンパイラに検出させる
2. ありえない状態をデータベース制約で物理的に作れなくする

---

## 2. 確定事項

以下は合意済み。**独断で変更しないこと。**

### 2.1 業務要件

| 項目 | 決定 | 出典 |
| --- | --- | --- |
| 利用形態 | 架空の 1 社向け社内システム。マルチテナントは扱わない | 合意 |
| 労働時間制度 | 固定時間制 + フレックスタイム制（清算期間 1 か月、コアタイム 11:00–15:00） | 要件定義書 3.1 |
| 労働時間の集計単位 | **1 分単位。丸めを行わない** | 要件定義書 BR-01 |
| 休憩 | 打刻で記録する。法定休憩に不足する場合は警告のみ | 要件定義書 BR-08 |
| 組織 | 部署ツリー（本部 → 部 → 課）。所属は有効期間を持つ | 要件定義書 BR-11 |
| 承認 | BR-11 が定める承認者による 1 段階承認。自己承認は禁止し、遡っても得られない場合は人事が承認する | 要件定義書 BR-11 |
| 兼務 | **扱わない。** 1 社員の所属は同時に 1 部署。ただし部署長の兼任は認める | 要件定義書 BR-11 補足 |
| 雇用形態 | **持たない。** 全社員の所定労働時間は 1 日 8 時間。短時間勤務は対象外 | 要件定義書 3.1 |
| 認証 ID | **社員番号**。メールは退職者の再割り当てと衝突するため使わない | 要件定義書 7 |
| 締め | 月末締め。人事担当が実行。締め後は変更不可 | 要件定義書 BR-10 |
| 年次有給休暇 | **付与基準日は入社日**（法定どおり）。出勤率 8 割で付与、時効 2 年、消化は先入先出、1 日単位のみ | 要件定義書 BR-14〜BR-17 |
| 比例付与 | **対象としない。** 全社員 1 日 8 時間・短時間勤務は対象外なので**該当者が存在しない**。扱うなら BR-04・BR-05 と併せて要件から改訂する | 要件定義書 BR-14 |
| 年 5 日の取得義務 | **可視化までを担う。使用者による時季指定は実装しない。** 未達でも取得・提出・承認・締めを止めない | 要件定義書 BR-17 |
| 認証・認可 | Spring Security（セッション） + ロール認可。**ログインは `POST /api/sessions`（既定のフォームログインを使わない）。`APPROVER` は認証時に部署長の事実から導出する** | 要件定義書 4 / API 設計書 3.9 |

業務ルールの詳細は [要件定義書](doc/01_要件定義/要件定義書.md) の 6 章（BR-01〜BR-12）が正。
**実装とテストは必ずこの ID を参照すること。**

### 2.2 技術スタック

| 領域 | 採用 | 備考 |
| --- | --- | --- |
| 言語 | Java 21 | `record` / `sealed interface` / パターンマッチングを積極的に使う |
| フレームワーク | Spring Boot 4.1.1 | 3.5 系は OSS サポート終了済み |
| ビルド | Gradle 9.7 (Kotlin DSL) | |
| DB | **PostgreSQL 16** | `EXCLUDE` 制約・範囲型・パーティション・配列型・生成列を使う。設計書の DDL はすべて 16 で検証しているので、採用版を 16 にそろえる |
| マイグレーション | Flyway | |
| O/R マッパー | Spring Data JPA | `infrastructure` 層の内側でのみ使う。**追記専用・まるごと入れ替えの表（打刻・日次勤怠の内訳）は `JdbcTemplate`**（同一性の管理も変更の追跡も要らないため） |
| テスト | JUnit 5 / AssertJ / Testcontainers / ArchUnit | 統合テストで H2 は使わない |
| フロントエンド | React 19 / TypeScript / Vite | |
| パッケージ | `jp.co.sample.kintai` | Group `jp.co.sample` / Artifact `kintai` |

**Lombok は使わない。** `record` があれば getter・equals・hashCode は不要であり、
`@Data` はドメインオブジェクトを可変にする方向に働くため。

### 2.3 アーキテクチャ

| 項目 | 決定 | 根拠 |
| --- | --- | --- |
| パッケージ構成 | **業務コンテキスト優先**。`attendance` / `employee` / `workrule` / `approval` / `shared` を最上位に置き、その中を 4 層に分ける | [ADR 0001](doc/05_ADR/0001_パッケージを業務コンテキスト優先で分割する.md) |
| ドメインと永続化 | **分離する**。ドメインは `record`、永続化は `@Entity`、両者を変換する | [ADR 0002](doc/05_ADR/0002_ドメインモデルとJPAエンティティを分離する.md) |
| 層の依存 | `presentation → application → domain`、`infrastructure ⇢ domain`（ポートを実装） | アーキテクチャ設計書 3.3 |
| トランザクション境界 | `application` 層の公開メソッド。`open-in-view` は無効 | アーキテクチャ設計書 6.1 |
| 例外 | ドメイン例外 → `@RestControllerAdvice` → RFC 9457 (Problem Details) | アーキテクチャ設計書 6.2 |
| 時刻 | ドメインは `LocalDateTime`（壁掛け時計）、DB は `timestamptz`。変換は `BusinessZone` に集約 | アーキテクチャ設計書 6.3 |
| 構造の強制 | ArchUnit（AR-01〜AR-09）。違反したらビルドを落とす。**ルール自体を `ArchRuleSelfTest` が検査する**（わざと違反したクラスに当てて落ちることを確かめる） | アーキテクチャ設計書 8 |
| 業務エラーの表現 | `shared.domain.DomainException` を継承し、`errorCode()` で Problem Details の `type` を返す。実装の不備（null・桁あふれ）は継承させない | アーキテクチャ設計書 6.2 |
| 取りうる値が法で固定された概念 | `enum` にする。`record` + 実行時検証だと、型の上では不正な値を渡せる（`NightWindow` がこの形） | CLAUDE.md 4.3 |
| BR-11 の責務分割 | `employee.OrganizationChart` が**事実の導出**、`approval.ApproverPolicy` が**承認の業務ルール** | アーキテクチャ設計書 6.4 |
| 期間の表現 | **すべて半開区間 `[from, toExclusive)`**。退職日は最終在籍日なので `plusDays(1)` で変換する | 社員・組織 ドメインモデル設計書 2.2 |
| 時刻の生成元 | `created_at` / `updated_at` は **DB の `now()`**（監査のため偽装できない）、業務上の日時は **`Clock`**（テストで固定するため） | 社員・組織 DB設計書 2.2 |
| コンテキスト間の逆向き問い合わせ | **ポートを `shared.domain` に置き、実装を提供側に置く。** 新しい辺を増やさない。`MonthClosureQuery`（締め判定）がこの形 | アーキテクチャ設計書 4 |
| 共有語彙の置き場所 | 2 つ以上のコンテキストが使う概念（`PremiumType`・`EmployeeId`・`DateRange`）は `shared.domain` へ。片側に置くと依存が循環する | アーキテクチャ設計書 4 |
| 履歴を持つマスタ | **系列（不変の識別子）と版（有効期間つき）に分ける。** 参照する側は系列を指す。就業規則がこの形 | 就業規則 DB設計書 2.1 |
| 適用開始日 | 月初日、**または当該社員の入社日**。月初日だけに限ると月中入社の初月が締められない | 就業規則 API設計書 2.3 |
| 法定値の制約 | 下限だけでなく **上限**も置く。法定労働時間・深夜帯を自由に動かせると割増の対象そのものが消える | 就業規則 DB設計書 2.2 |
| 設計書を書き終えたら | [設計規約チェックリスト](doc/02_詳細設計/00_共通/設計規約チェックリスト.md) を上から確認してからコミットする | レビュー第 2 回 |
| **実装を変えたら** | **その根拠になった設計書の節を同じコミットで直す。** 別コミットに回すと必ず片方だけが古くなる | レビュー第 3 回 |
| 永続化アダプタ | ドメインの record と JPA エンティティを変換する。エンティティは**パッケージプライベート**で層の外に出さない | ADR 0002 |
| 番兵と NULL の写像 | `infrastructure` の責務。`DateRange.UNBOUNDED_END` は PostgreSQL の `date` に入らないので、1 か所（`Periods`）に閉じる | CLAUDE.md 落とし穴 35 |
| 時点解決 | **SQL で書き直さない。** 行を読んでドメイン（`EffectiveWorkRule`）へ渡す。2 か所に実装があると、どちらが正しいかを 2 か所で保つことになる | レビュー第 3 回 |
| 判別値と分岐 | 分岐は `sealed interface` に対する網羅性検査つき `switch`。enum（`WorkingTimeSystemType` など）は**永続化と表示のためだけ** | 就業規則 ドメインモデル設計書 2.1 |
| API の応答 | **他コンテキストが所有する概念を返さない。** 社員番号・氏名・部署は `employee` から引く | 設計規約チェックリスト 3 |
| 依頼者の受け渡し | **`shared.domain.Requester` を引数で渡す。** `SecurityContextHolder` を `application` 層から読まない。読むとユースケースが「誰の依頼か」を差し替えられなくなり、バッチからも呼べなくなる（落とし穴 42） | M1-c/A |
| 閲覧範囲の判定 | **`shared.domain.EmployeeVisibility`（ポート）。実装は組織を持つ `employee` 側。** 「配下部署か」は組織と基準日に依存するので Spring Security の認可設定には置けない | 要件定義書 4.1 |
| 業務エラーの HTTP 対応 | ドメインは `DomainErrorKind`（業務上の分類）だけを持つ。HTTP への対応づけは `presentation` の網羅性検査つき `switch`。`RULE_VIOLATION`=422 / `CONFLICT`=409 / `NOT_FOUND`=404 / `FORBIDDEN`=403 | アーキテクチャ設計書 6.2 |
| 実装の不備の応答 | `IllegalArgumentException` などは**メッセージを応答に載せない。** ログへ出して 500。制約名やテーブル名は内部の構造であり外に出さない | アーキテクチャ設計書 6.2 |
| 締め状態の問い合わせ | **`MonthClosureQuery` は「打刻を受け付けるか」と「訂正申請を受け付けるか」を分ける。** 1 つにまとめると提出済みの月で真になり、**本人が提出後に直接打刻できてしまう**（承認者が見た内容と確定する内容が食い違う） | 05 ドメインモデル設計書 2.1 |
| 月次清算の再計算 | **人事の指示のときだけ版を突き合わせる。** システムが契機で行う再計算（提出・訂正の承認）は誰かが見ている値ではないので、突き合わせる相手がいない | 04 API 設計書 3 |
| 打刻の成否と計算の成否 | **分けて返す**（`calculationStatus`）。就業規則の未設定を理由に打刻を拒否すると、働いた事実が残らない | CLAUDE.md 落とし穴 19 |
| 勤務日と暦日 | 労働時間の帰属は**勤務日**（BR-03）、法定休日労働の判定は**暦日** | 日次集計 ドメインモデル設計書 2.4 |
| 要件に無い概念 | 設計で必要になったら、**要件を先に改訂する**。要件定義書は版 1.0 | レビュー第 2 回 |
| 楽観ロックの版 | **行の作成時に 1 を入れる。** 既定値の 0 に任せると「行が無い」と「作られたばかり」が同じ値になり、古い版がそのまま一致する | CLAUDE.md 落とし穴 57 |
| 一括操作の失敗 | **社員ごとの事情は結果へ、依頼そのものの不備は例外へ。** 前者を例外にすると 1 件で全件が巻き戻り、後者を結果にすると権限不足に気づけない | 申請承認 API設計書 2.7 |
| 一括操作の実装場所 | **別のクラスに置く。** 同じクラスから `@Transactional` を呼ぶとプロキシを通らず、1 トランザクションになる | CLAUDE.md 落とし穴 59 |
| 訂正の代理申請 | **認めない。** 訂正は本人の意思表示であり、人事でも代わりには出せない。認めると「本人が申請していない訂正」が生まれる | 申請承認 API設計書 3.1 |
| シナリオテストの経路 | **HTTP を通す。** アプリケーションサービスを直接呼ぶと 1 シナリオが 1 トランザクションになり、締めの排他と楽観ロックが検証できない | 結合テスト仕様書 7 の #2 |
| 全社共有の表の締め判定 | **`isClosedForAnyone` を分けて置く。** カレンダーは 1 つしか無いので、社員単位の判定では他人が締めた月を守れない | CLAUDE.md 落とし穴 72 |
| 他コンテキストの `application` | **図にある辺に沿うかぎり呼んでよい**（AR-06 は `infrastructure` / `presentation` だけを禁じる）。複数のリポジトリを束ねる手順は `domain` に置けず、写すと片方が古くなる（落とし穴 67）。逆向きはポートを `shared.domain` に置く | アーキテクチャ設計書 4 |
| 一覧に版を載せるか | **詳細だけに載せる。** 一覧で行ごとに引くと、社員数ぶんの問い合わせと認可判定が重複する。版が要るのは更新する 1 人だけ | 社員・組織 API設計書 3.12 |
| 訂正の承認者 | **`workDate` が属する月の承認者。** 月次と食い違うと「月次は承認できるが訂正は承認できない」上長が生まれる | 申請承認 ドメインモデル設計書 4.1.2 |
| 締め済みと承認済み | **別のエラーにする**（`month-already-closed` / `month-not-editable`）。承認済みは承認を取り消せば直せるので、利用者への案内がまったく違う | 申請承認 API設計書 3.1 |

詳細は [アーキテクチャ設計書](doc/02_詳細設計/00_共通/アーキテクチャ設計書.md)。

---

## 3. ディレクトリ構成

```
.
├── CLAUDE.md               このファイル
├── doc/                    設計文書（工程ごとに番号付き）
│   ├── README.md           文書の読み方
│   ├── _tools/             図・テスト一覧・マイグレーションの生成ツール
│   ├── 00_開発環境構築手順.md
│   ├── 01_要件定義/
│   ├── 02_詳細設計/        00_共通 と業務コンテキストごとに細分化
│   ├── 03_単体テスト/
│   ├── 04_結合テスト/
│   ├── 05_ADR/             設計判断の記録
│   └── 06_レビュー記録/    レビューの指摘と対応
├── src/
│   └── backend/            Java (Spring Boot)。frontend は M1-c 以降に作る
├── docker-compose.yml
├── .editorconfig
└── .gitattributes
```

---

## 4. 規約

### 4.1 ドキュメント

| 規約 | 内容 |
| --- | --- |
| 形式 | Markdown |
| **図** | **Mermaid のソース（`diagrams/*.mmd`）を正とし、PNG を生成して埋め込む。** 開いた瞬間に図が見える状態を保つ |
| 図の生成 | `cd doc/_tools && npm run render`。**PNG を直接編集しない** |
| **テストケース一覧** | **詳細設計書を正とし、`cd doc/_tools && python3 collect-tests.py` で生成する。一覧を直接編集しない** |
| **マイグレーション** | **DB設計書を正とし、`cd doc/_tools && python3 build-migrations.py` で生成する。`db/migration/*.sql` を直接編集しない** |
| 粒度 | 業務コンテキスト単位に細分化する。1 文書が長大にならないようにする |
| ID | 要件は `BR-xx`、アーキテクチャ規約は `AR-xx`、単体テストは `UT-xx`、結合テストは `IT-xx`。**ID はシステム全体で一意**（コンテキストごとに振り直さない） |
| 追跡可能性 | 要件 → 設計 → テスト → 実装 を ID で辿れる状態を保つ |

### 4.2 コミットメッセージ

```
1 行目に何をしたかを日本語で簡潔に

3 行目以降に、なぜそうしたかを書く。
何をしたかは差分を見れば分かるが、理由は書かないと残らない。
```

### 4.3 コード

| 規約 | 内容 |
| --- | --- |
| インデント | Java は 4 スペース、その他は 2 スペース（`.editorconfig` に定義） |
| 文字コード / 改行 | UTF-8 / LF（`.bat` のみ CRLF） |
| ドメインのクラス | 不変。`record` を第一選択とする |
| 種類が固定された概念 | `sealed interface` で表現し、`switch` の網羅性検査を効かせる |
| 不変条件 | compact constructor で検証し、不正なインスタンスを存在させない |
| JPA エンティティ | パッケージプライベート。`infrastructure` の外に出さない |
| 依存の注入 | コンストラクタインジェクション。フィールドインジェクション禁止 |
| 現在時刻 | `Clock` を DI する。`LocalDateTime.now()` を直接呼ばない |

### 4.4 Git

- 開発ブランチ: `claude/java-personal-project-ideas-f08w42`
- **履歴の書き換え（force push）は行わない。** 複数環境から更新するため
- `gradlew` の実行権限に注意（Windows でコミットすると落ちる）

---

## 5. 既知の落とし穴

過去に踏んだ、または踏みかけた問題。**同じ轍を踏まないこと。**

| # | 内容 |
| --- | --- |
| 1 | **`hibernate.jdbc.time_zone` を設定しない。** JDBC 層が `time` 型のカラムまで変換し、深夜帯 22:00–05:00 が別の時刻として読み込まれる |
| 2 | **Spring Boot 4 でスターター名が変わっている。** `spring-boot-starter-web` → `spring-boot-starter-webmvc`、Flyway は `spring-boot-starter-flyway`、テストは機能ごとに `-data-jpa-test` などへ分割。3.x 向けの記事をそのまま写さない |
| 28 | **Spring Boot 4.1 が管理する Testcontainers は 2.0 系だが、`junit-jupiter` / `postgresql` の座標が 1.x から変わっており解決できない。** BOM を 1.21.3 に固定する |
| 29 | **ArchUnit のルールのフィールド名に日本語を使わない。** フィールド名がそのままレポートのファイル名になり、locale が POSIX / C の環境（CI のコンテナ）で「Malformed input」でレポート生成が落ちる。`sun.jnu.encoding` は `-D` で上書きできない。説明は `because(...)` に書く |
| 30 | **ArchUnit の `noClasses()` は、対象が 0 件だと「1 件も検査しなかった」として落ちる。** 実装が進んでいない段階では `allowEmptyShould(true)` を付ける |
| 31 | **ArchUnit のルールは、わざと違反するクラスを置いて落ちることを確かめる。** 対象が無くて通っているのか、守れていて通っているのかを区別する |
| 3 | **`.gitignore` でスラッシュを含むパターンはその `.gitignore` の階層が基準。** `!gradle/wrapper/gradle-wrapper.jar` はサブディレクトリのプロジェクトに効かない。`!**/gradle/...` と書く |
| 4 | **`./gradlew build` はテストを含む。** データソース未設定の段階では `contextLoads()` が失敗する。生成直後の確認は `compileJava` で行う |
| 5 | **深夜は他の区分と重複する属性であり、労働時間を分割する区分ではない。** 集計時に「深夜のみが付いた所定内の区間」を数え損ねやすい。内訳の合計＝実労働時間、を不変条件として検証すること |
| 6 | **統合テストで H2 を使わない。** `EXCLUDE` 制約・パーティション・配列型が再現できず、通っても意味がない |
| 7 | **`EXCLUDE USING gist` には `CREATE EXTENSION btree_gist` が必須。** uuid の等値比較と範囲型の重なり比較を同居させるため。Flyway の初回マイグレーションの先頭に置く |
| 8 | **`CHECK` 制約に副問い合わせは書けない。** 配列の要素数を数えるような検証は `CASE WHEN ... = ANY(...)` の加算で書く |
| 9 | **`BEFORE` トリガは `CHECK` 制約より先に評価される。** どちらでも防げる不正データは、トリガのメッセージが返る |
| 10 | **閉区間と半開区間を混在させない。** 在籍期間（閉区間の感覚）と所属期間（半開区間）が混ざると、退職日当日の 1 日が消える |
| 11 | **自分で作った検証データを疑う。** 承認者不在の欠陥は、自分の検証結果に現れていたのに見逃していた |
| 12 | **検証では入力を 1 つだけ変える。** 2 つ変えると何を証明したのか分からなくなる |
| 13 | **有効期間を持つマスタを、参照側が「版」で指さない。** 改定した瞬間に全参照が切れる。系列を指させる |
| 14 | **同じ UUID でも意味が違うなら型を分ける。** `WorkRuleId` と `WorkRuleSeriesId` が同じ型だと取り違えてもコンパイルが通る |
| 15 | **法定値には上限も置く。** 割増率の下限だけ守っても、法定労働時間を 12 時間にされたら割増の対象が消える |
| 16 | **成立しない CHECK 条件を書かない。** 所定 ≤ 480 分を別の制約で保証しているなら「480 分超なら休憩 60 分」は永遠に成立しない |
| 17 | **検証が「拒否された」ことだけを見ない。** 期待した制約で拒否されたかを確認する。別の制約に先に引っかかると、狙った検証は行われていない |
| 18 | **同じ指摘が 2 回出たら、それは規約になっていない。** 個別に直さず [設計規約チェックリスト](doc/02_詳細設計/00_共通/設計規約チェックリスト.md) に項目を足す |
| 19 | **一次証拠の記録を、別の日の不整合や計算側の都合で止めない。** 打刻を拒否すると働いた事実が残らない |
| 20 | **勤務日と暦日は別のもの。** 労働時間の帰属は勤務日（BR-03）、法定休日労働の判定は暦日 |
| 21 | **採用する DB のバージョンと検証に使うバージョンをそろえる。** 検証できない版を設計書に書かない |
| 22 | **制度の分岐を暦日区分の分岐と混ぜない。** フレックスに日次の残業を付ける誤りは、`switch (dayType)` だけで書いたことから生まれた |
| 23 | **排他を主張する CHECK は、両方が正になる正当なケースが無いか先に探す。** 見落とすと適法なデータを保存できなくなる |
| 24 | **正常系の検証を 1 行にまとめない。** 「正常な FIXED / FLEX」を 1 行にしたことで、所定 > 総枠 の月の欠陥を見逃した |
| 25 | **「拒否された」ではなく「狙った制約で拒否された」ことを確認する。** 別の制約に先に引っかかると、狙った検証は行われていない |
| 26 | **実行者が不在になる経路を洗い出す。** 退職・部署廃止で提出・承認・締めが誰にもできなくなる状態を 2 回作った |
| 27 | **副作用として自動で起きる状態遷移も型にする。** そうしないと監査証跡に残せない |
| 32 | **秒の丸めを区間ごとに独立して適用しない。** 休憩開始を切り上げ・休憩終了を切り捨てると、1 分未満の休憩では直前の区間の終了が次の区間の開始より後になり、**区間が重なって労働時間が二重に計上される。** 乱数 20,000 件のうち 10,222 件が該当した。丸めた開始が直前の終了より前なら押し戻す |
| 33 | **拘束時間を実労働区間から求めない。** 出勤直後や退勤直前の休憩で区間が長さ 0 になって捨てられ、出退勤の時刻が失われる。出勤・退勤の打刻から直接求める |
| 34 | **`record` の `equals` は `BigDecimal` の<ruby>スケール<rt>小数の桁数</rt></ruby>も比較する。** DB の `NUMERIC(4,3)` は `0.250` を返すので、正規化しないと定数 `0.25` と等しくならず、読み書きを往復しただけで別の値になる |
| 35 | **`LocalDate.MAX` は PostgreSQL の `date` に入らない**（上限は 5874897 年）。番兵として使うなら、`infrastructure` が `NULL` に写す責務を明示し、日数計算は例外にする。素通りさせると 365241759927 日という値が黙って返る |
| 36 | **compact constructor が強制している不変条件を、その型のインスタンスに対して確かめない。** 破れていれば生成の時点で例外になるので、アサーションは**恒真**である。強制している当の場所に破れた値を渡す |
| 37 | **テストの代役（テストダブル）に業務ロジックを持たせない。** 日数の数え方を代役側に置くと、そのテストは本番のコードを 1 行も検査しない。既定実装をインタフェースに置き、代役は事実だけを答える |
| 38 | **実装を直したら、根拠になった設計書の節も同じコミットで直す。** 秒の丸めの欠陥を修正したとき Java だけを変え、設計書は「逆転はしない」と書いたまま残った。**修正済みの欠陥を設計書がなお正しいと主張する**状態になる |
| 39 | **導出できる値を列として持つなら、食い違いを DB で禁じる。** `calendar_date` は開始時刻から導けるのに、無関係な値を保存できた。集計は開始時刻で、法定休日の判定は `calendar_date` で行われて結果が食い違う |
| 40 | **長さ 0 の区間を作れない型に、長さ 0 になりうる値を渡さない。** 出勤と退勤が同一時刻だと拘束時間の生成が例外になり、`DomainException` ではないので 500 になって**打刻の登録そのものが落ちた** |
| 41 | **同時刻の打刻に固定の全順序を与えない。** 「休憩終了 → 休憩開始」（休憩を 2 つに割った境界）と「休憩開始 → 休憩終了」（長さ 0 の休憩）の両方が正当であり、一方にそろえると他方を拒否する。状態機械が受け入れられる打刻を先に採る |
| 42 | **「誰の」を引数で受け取らない解決関数を作らない。** `resolve(assignments, versions, date)` は他人の適用を渡されると**他人の規則を黙って返し**、複数人ぶんを渡されると「制約が壊れた」と誤診断した |
| 43 | **ミューテーションテストは実際に走らせる。** 「このテストがあるから守れている」は思い込みで、16 件試して多くが生存した。とくに**閾値をまたがない数字**を選んだテストは、ルールを消しても落ちない（落とし穴 24 と同型） |
| 44 | **PNG は再生成しても毎回バイト差分が出るものがある**（23 枚中 5 枚）。「`npm run render` して差分が出ないこと」で最新性を判定できない。`.mmd` を変えたときに再生成する運用で担保する |
| 45 | **生成器が拾えない ID を書かない。** `UT-WR-12b` のような接尾辞つき ID は `collect-tests.py` の正規表現に合わず、**黙って一覧から落ちる**。コードにしかない ID は追跡可能性の穴になる |
| 46 | **Spring Boot 4 は Jackson 3 を使う。** `ObjectMapper` のパッケージが `com.fasterxml.jackson.databind` から **`tools.jackson.databind`** へ変わっている（注釈は `com.fasterxml.jackson.annotation` のまま） |
| 47 | **`HttpStatus.UNPROCESSABLE_ENTITY` は非推奨。** RFC 9110 で 422 の名前が Unprocessable Content に変わった。`UNPROCESSABLE_CONTENT` を使う。**`-Xlint:deprecation` を有効にしていないと場所が分からない** |
| 48 | **Spring Data はクラスの内側に入れ子にしたインタフェースを走査しない。** リポジトリを 1 ファイルにまとめると Bean が作られず、コンテキストの起動ごと失敗する |
| 49 | **勤務日を打刻した暦日で決めない**（BR-03）。日跨ぎ勤務の退勤が翌日の勤務として記録され、**出勤の無い日に退勤だけが残る**。開いている勤務日を探して、そこへ追記する |
| 50 | **例外に HTTP のステータスを持たせない。** ドメインはフレームワークを知らない（AR-01）。業務上の分類（`DomainErrorKind`）だけを持たせ、HTTP への対応づけは `presentation` の網羅性検査つき `switch` で行う。文字列の対応表で引くと、追加した例外が 500 で黙って漏れる |
| 51 | **不変条件を、それが成り立つ根拠ごと確かめずに制度をまたいで適用しない。** 「時間外と不足は同時に正にならない」はフレックス（両方を同じ対象労働時間から導く）でしか成り立たず、固定時間制（時間外は日次・週次の実績、不足は所定総との差）では**忙しい週に残業して別の週に欠勤しただけの正当な月**を保存できなくなる。落とし穴 23 の再発 |
| 52 | **限度時間（月 45 時間・年 360 時間）に休日労働を数えない。** 36 条 3 項・4 項の対象は時間外労働だけで、休日労働を含めるのは 6 項 2 号・3 号（単月 100 時間未満・複数月平均 80 時間以内）という**別の規制**である。混ぜると、時間外 44 時間 + 法定休日 8 時間という**適法な月に偽の警告**が立ち、逆に 6 項違反を「限度時間の超過」と誤って伝える |
| 53 | **清算期間を通算する式を固定時間制に当てない。** 所定は 1 日ごとに約束されているので、19 日 8 時間・1 日 12 時間・1 日欠勤の月に「所定総 − 実労働」を当てると不足が 8 時間ではなく 4 時間になり、**残業が欠勤控除を相殺する。** 同じ 4 時間が「割増の対象」と「欠勤の穴埋め」に二重に使われる |
| 54 | **週や月をまたぐ集計を「末日が属する月」へ寄せない。** その月の清算が行われるとは限らない。月末が金曜の月に退職すると、最終週の週 40 時間超が存在しない翌月に割り当てられ**誰にも計上されない**。発生した暦日で振り分ける |
| 55 | **テストダブルが本番の分類を写経していないか確かめる。** 「本番の計算を通して作る」と javadoc に書きながら実際は通していない、という状態を作っていた。閾値の分割を代役側に持つと、日次と月次の継ぎ目を 1 行も検査しないテストになる（落とし穴 37 の再発） |
| 56 | **1 区間 177 時間のような、本番では決して現れない日次をテストで作らない。** 暦日境界も深夜帯もまたがないので、境界の処理を一切通らないまま「通った」ことになる。代役に上限を持たせて作れなくする |
| 57 | **楽観ロックの版を 0 から始めない。** `monthly_attendances` の行は提出時に初めて作られるので「行が無い月」と「作られたばかりの月」があり、どちらも版 0 だと**区別できない。** 提出前の画面（版 0）を握った承認者の要求が、提出後もそのまま一致して**楽観ロックが素通りする。** 行の作成時に 1 を入れ、0 は「行が無い」ことだけを指す値にする |
| 58 | **集約が持つ不変条件を、アプリケーション層を通してだけ検査しない。** 自己承認の禁止は `ApproverPolicy` の承認者判定で先に弾かれるため、**API のテストからはこの検査が一度も働かない。** 実際に禁止を削除しても API のテストは 1 件も落ちなかった。集約に直接あてるテストを書く |
| 59 | **同じクラスの中から `@Transactional` のメソッドを呼ばない。** Spring のプロキシを通らないので**トランザクションが始まらず**、一括締めが 1 つのトランザクションになる。1 件の失敗で 99 件の締めが巻き戻る。別のクラス（`BulkClosureService`）に分ける |
| 60 | **「一括」の失敗を 1 種類にしない。** 社員ごとの事情（未提出・未承認）は結果の `skipped` に、依頼そのものの不備（人事でない・対象月が終わっていない）は例外に分ける。全員を `skipped` にすると、人事は**自分に権限が無いことに気づけない** |
| 61 | **設計書がコンパイル時に保証すると書いた不変条件を、実装が実行時の検査にしていることがある。** 締め済みからの遷移は「型として存在しない」と書いていたが、遷移メソッドを状態ではなく集約に置いた（自己承認の判定に対象社員の ID が要る）ため `closed.reject()` はコンパイルが通る。**設計書が「テストは書けない」と言っている観点こそ、実装を確かめる** |
| 62 | **API のテストを設計書の ID に登録し忘れる。** `IT-APV-30`〜`47` はテストコードにしか無く、一覧から漏れていた。要件 → 設計 → テスト → 実装 の追跡が切れる（落とし穴 45 と同型）|
| 63 | **テストの前提が、狙った分岐をまたいでいるか確かめる。** 「退職者も一括締めの対象に含める」検査で退職日を 4/30（＝月末時点でまだ在籍）にしたため、在籍者だけに絞る変異が生き残った。月中退職（4/15）にして初めて検査になった（落とし穴 24・43 と同型）|
| 64 | **同じ値を 2 か所に持たせない。** 訂正の `Revoke` に `workDate` を持たせる設計だったが、申請そのものも `workDate` を持つ。食い違ったときにどちらが正しいか決められないので、申請の側にだけ置く |
| 65 | **打刻に識別子を持たせない。** 労働時間の計算に要るのは種別と時刻だけで、識別子を持たせると値としての等価性（同じ時刻の同じ種別は同じもの）が壊れる。識別子が要るのは訂正だけなので `RecordedTimeClockEvent` で外から添える |
| 66 | **DB の外部キー違反を業務エラーの代わりにしない。** 実在しない打刻を取消対象にした申請は `correction_items_target_fk` でも弾かれるが、**外部キー違反は利用者に説明できない。** 申請の時点で確かめ、どの打刻が見つからないかを返す |
| 67 | **再計算の手順を 2 か所に書かない。** 訂正の承認は「打刻から日次を作る」を必要とするが、これは打刻の登録がすでに持っている。`TimeClockService.recalculate` として 1 つだけ置き、訂正側から呼ぶ。写すと丸めや勤務日の扱いを直したときに片方だけが古くなる |
| 68 | **開いている勤務日に、出勤打刻まで吸わせない。** 前日の退勤を打ち忘れると、開いた勤務日へ翌朝の出勤が追記されて状態機械が壊れ、**その社員は翌朝そもそも打刻できなくなる。** 出勤は必ず新しい勤務日を始め、打ち忘れた日は警告として返す（落とし穴 19 の再発） |
| 69 | **状態機械の判定を SQL に写さない。** 「未退勤の勤務日」を `HAVING sum(CLOCK_OUT) = 0` で書くと、`isClosed()` と 2 か所に分かれる。SQL は候補を返すところまでにとどめ、判定はドメインに任せる（落とし穴 13・時点解決と同型） |
| 70 | **提出の事前条件を暦月で見ない。** 在籍していない日（入社前・退職後）の勤務日まで「日次勤怠が未確定」と数えると、その日に働くことはありえないので確定させる手段が無く、**月中入社の初月と月中退職の最終月を永久に提出できない。** 在籍期間との重なりで絞る |
| 71 | **設計書が「提出処理の内部で行う」と書いた再計算を省かない。** 提出時に月次清算を計算し直していなかったため、承認者は日次だけが直った古い月次を見て承認することになっていた。単体でも API のテストでも捕まらず、通しのシナリオで初めて出た |
| 72 | **全社で共有する表の変更を、社員単位の締め判定で守らない。** 会社カレンダーは 1 つしか無いので、特定の社員が未締めでも他の社員が締めていれば変更してはいけない。`isClosedForAnyone` を分けて置く |
| 73 | **有効期間を持つ行の「変更」を、挿入だけで実装しない。** `work_rule_assignments` に入れるだけだと期間が重なって EXCLUDE 制約に弾かれ、**一度適用した社員の就業規則を二度と変更できない。** 現行を閉じてから入れるまでを 1 つの操作にする（就業規則の改定と同型） |
| 74 | **落とし穴 29 は Java の型名とテストメソッド名にも当てはまる。** 日本語にすると `.class` とレポート HTML のファイル名になり、POSIX / C ロケールで書き出しに失敗する。業務の言葉は `@DisplayName` とヘルパのメソッド名に置く |
| 75 | **「その日に在籍していたか」と「名簿に載せるか」を 1 つのメソッドにまとめない。** 前者は入社日で絞るので**未来日入社の社員が一覧に現れず**、管理者が登録の成否を確認できない。名簿は退職者を除くかどうかだけで絞る |
| 76 | **`@JsonInclude(NON_NULL)` を record 全体に付けない。** 省きたいのが 1 項目でも、`department` や `retiredOn` の `null` まで消え、**「所属が無い」ことを応答から読み取れなくなる。** 項目ごとに付ける |
| 77 | **正規化を型が持っているなら、比較する側で畳み直さない。** `Email` は生成時に小文字化しているのに、重複検査でも `equalsIgnoreCase` を使っていた。同じ規則が 2 か所にあると変異が等価になり、**正規化を消してもテストが落ちない場所**が生まれる |
| 78 | **退職で部署長を閉じ忘れない。** 所属だけ閉じても、**その部署に所属する全社員の承認者が退職者になり続ける。** 既存の行が後から不正になる種類の問題なので、DB の制約では検出できない（落とし穴 26 と同型） |
| 79 | **期間を「開き直す」ときは、閉じた日で絞る。** 社員 ID だけで開き直すと、異動で閉じた過去の所属まで開いてしまい、期間が重なって排他制約に弾かれる。退職の取消が巻き戻すのは、退職で閉じたぶんだけ |
| 80 | **`Stream.findFirst()` は先頭が null だと NPE を投げる。** 「開いている期間」を表すのがまさに `valid_to IS NULL` なので、`queryForList(...).stream().findFirst()` で取り出すと落ちる。添字で取り出す |
| 81 | **PL/pgSQL の `RAISE` は制約違反ではない。** 循環検出トリガの拒否は `DataIntegrityViolationException` にならず Problem Details へ写らないので、**親の選び間違いが 500 になる。** 業務エラーは `application` 層で検出し、トリガは最後の防波堤として残す |
| 82 | **テストの接続プールを既定のままにしない。** `@TestBean` で時計を差し替えるたびに別のコンテキストが作られ、そのそれぞれが 10 本の接続を握る。10 個ほど積み上がると `max_connections` に届き、**後から起動したコンテキストが「too many clients」で丸ごと落ちる。** コード側の回帰と見分けがつかない |
| 83 | **親だけを廃止しない。** 配下に現存する部署が残ると、子が親のいない部署になり、承認者の遡りが根へ到達できなくなる（落とし穴 26 と同型） |
| 84 | **禁止先が複数ある ArchUnit ルールは、禁止先ごとに違反クラスを置く。** AR-06 は `infrastructure` と `presentation` の 2 つを禁じているのに、踏んでいたのは前者だけだった。**禁止先から `presentation` を削っても自己検査が通る**状態で、ルールが半分死んでも気づけない（設計書 9 章がまさにこれを警告していた） |
| 85 | **ルールの javadoc がルール自体より強いことがある。** AR-06 の説明は「参照してよいのは domain だけ」と書いていたが、設計書も実装も `application` を認めている。**読んだ人が実装のほうを疑う**ので、説明は必ずルールに合わせる |
| 86 | **「禁止先ごとに違反クラスを置く」という規約は、書いてあるだけでは守られない。** 設計書に明記していたのに、実際は AR-06 の `presentation`・AR-03 の `presentation`・AR-10 の 3 コンテキストが踏まれておらず、**禁止先を削っても自己検査が通る**状態だった。禁止先を 1 つずつ削って落ちるかを変異で確かめる |
| 87 | **違反クラスを書けない禁止先を置かない。** AR-01 は `io.swagger..` を挙げていたが springdoc は依存に入っておらず、効いているか確かめようがない。**「守られている」という見かけだけが増える。** 採用するときに違反クラスと一緒に戻す |

---

## 6. 現在地

| マイルストーン | 内容 | 状態 |
| --- | --- | --- |
| M0 | 開発環境の整備 | 完了 |
| 要件定義 | 要件定義書 | 完了 |
| 詳細設計 | アーキテクチャ / 各コンテキストの設計 | **完了**（00〜05・90_画面。レビュー 2 回・指摘 155 件を反映。DDL は PostgreSQL 16 で 104 件の制約検証を実施） |
| テスト設計 | 単体テスト仕様書 / 結合テスト仕様書 | **完了**（単体 126 件・結合 104 件 + シナリオ 12 件） |
| M1-a の土台 | Flyway / Testcontainers / ArchUnit / 制約テスト | **完了**（テスト 128 件が green。設計書の IT-ID 104 件を全数実装） |
| M1-a | マスタと固定時間制の日次計算 | **完了**（ドメイン・永続化アダプタ・API。テスト 407 件が green。Problem Details の変換、打刻の登録、日次勤怠の参照を MockMvc で API から DB まで通した） |
| M1-b | フレックスの月次清算、週 40 時間超、36 協定 | **完了**（ドメイン・永続化・結合。テスト 494 件が green。実装レビュー第 4 回の指摘 6 件を反映し、要件を 0.8 まで改訂） |
| M1-c | 認証・認可、打刻訂正、提出 → 承認 → 締め | **完了**（認証・認可、月次清算 API、提出 → 承認 → 締め、一括締め、楽観ロック、打刻の訂正申請） |
| M1-d | 積み残しの解消とシナリオ結合テスト | **完了**（`unclosedWorkDates`、カレンダー・規則適用の API と締めガード、シナリオ IT-SCN-01〜12。テスト 659 件が green。通しで初めて出た欠陥を 5 件修正） |
| M1-e | 社員・組織のマスタ保守 API | **完了**（社員名簿、異動・退職・ロール、部署ツリー。テスト 705 件が green。変異 33 件を確認） |
| M2 | 年次有給休暇 | 未着手 |
| M3 | シフトと外部連携 | 未着手 |

---

## 7. 未決事項

| # | 内容 | 決定時期 |
| --- | --- | --- |
| 1 | 祝日データの取得方法（内閣府の CSV 取込か手動登録か） | M1-a |
| 2 | 複雑な集計クエリを JPQL にするかネイティブクエリにするか | DB 設計書 |
| 3 | フロントエンドの API 型を手書きするか OpenAPI から生成するか | M1-a |
| 4 | 監査証跡をアプリケーションで記録するか DB トリガで記録するか | DB 設計書 |
| 5 | 給与計算システムへの連携形式 | M3 |
| 6 | ~~年次有給休暇の付与基準日~~ **解決済み。入社日基準**（BR-14）。一斉付与は初年度に法定どおりの前倒し付与が要り、結局「入社日基準ならいくらか」の計算を内部に持つことになる | 完了 |
| 7 | ~~パスワードの初期発行と再設定の運用~~ **解決済み。** 要件 BR-13 に定めた（`ADMIN` が発行・再設定し、口頭で伝える。メールでのリセットは設けない） | 完了 |
| 8 | ~~M1-a の時点では認証をかけていない~~ **解決済み。** M1-c/A で社員番号によるログインとロール認可を入れた。未認証は 401、CSRF は有効 | 完了 |
