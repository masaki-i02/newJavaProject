# 08_運用

**動かし方と、壊れたときの直し方を書く。**

設計書は「なぜそう作ったか」を残すが、
運用の手順は「いま何をすればよいか」を残す。読む場面が違うので分ける。

| 文書 | いつ読むか |
| --- | --- |
| [デプロイ手順書](デプロイ手順書.md) | 初回構築・版の更新・停止 |
| [バックアップとリストア](バックアップとリストア.md) | 毎日（自動）・DB を壊したとき |
| [障害時の初動](障害時の初動.md) | 動かなくなったとき |
| [年次作業](年次作業.md) | 毎年 12 月 |

## 前提

| 項目 | 決定 | 根拠 |
| --- | --- | --- |
| 構成 | **app 1 インスタンス + PostgreSQL 1 台。スケールアウトしない** | [ADR 0009](../05_ADR/0009_1インスタンスで運用する.md) |
| 規模 | 従業員 100 名・同時利用 50 名（要件 7）| 要件定義書 |
| SLA | **定義しない**（個人開発） | 要件定義書 7 |
| データ保持 | **5 年**（労働関係の記録） | 要件定義書 7 |

## この環境で検証できていないこと

**正直に書く。** 「手順があること」と「その手順が通ること」は別である
（CLAUDE.md 落とし穴 87）。

| 項目 | 状態 |
| --- | --- |
| `Dockerfile` のビルド | **未検証。** 開発に使っている環境からコンテナレジストリ（Docker Hub の blob 配信）へ到達できず、ベースイメージを取得できない |
| `docker compose -f docker-compose.prod.yml up` の通し | **未検証。** 同上 |
| バックアップ → 破棄 → リストアの実測 | **未検証。** コンテナを起動できないため |
| 死活監視の口 | **検証済み**（`ActuatorAccessTest` の IT-OPS-01〜04）|
| 本番プロファイルが既定値へ落ちないこと | **検証済み**（`ProdProfileTest` の IT-OPS-05〜07）|

**未検証のものは、初回構築のときに 1 回通して、結果をこの表へ書き戻すこと。**
とくにリストアは、**実際に 1 回戻して所要時間を実測するまで完了としない。**
手順が書いてあるだけのバックアップは、無いのとほとんど変わらない。
